# Architecture review — July 2026 **[CLOSED 2026-07-09]**

**This review cycle is closed.** Every finding below was re-verified as solved on 2026-07-09
(evidence greps re-run per finding + the full `go test -tags integration ./...` suite: 55 packages
ok, zero failures). The items that remained open — R‑13 (remainder), R‑14, R‑18, R‑19 — were
**carried to [review-2026-08.md](review-2026-08.md)**, which is the active review; new findings
continue there as R‑20+. This file stays as the historical record of the solved findings and the
measurements that proved them.

**Audience: Claude Code (Opus 4.8).** This document is written to be executed against, not just
read. Every finding carries: the evidence (file + symbol, so it survives line drift), why it fails
at the target scale, a fix sketch, and acceptance criteria. Re-verify a finding's evidence before
working on it — the anchors were correct as of 2026-07-05 (uncommitted work through M35/M44/M45 on
`main`).

**Scope reviewed:** the oikumenea Go backend (`internal/`, `cmd/oikumenea`), the DB schema +
Atlas migrations (`migrations/`), and the hermenea companion (`internal/hermenea`,
`cmd/hermenea`, `migrations/hermenea/`). The web console and client SDKs were out of scope.

**Scale target judged against:** multi-org national scale — ~1M+ persons in the shared
directory, unit graphs of 10⁴–10⁵ units per large org, heavy PDP query load (every request is
PDP-gated), reference datasets in the 10⁵–10⁷ row range.

**Stance on binding decisions:** `decisions.md` was treated as challengeable. Findings that
contradict a recorded decision say so explicitly and name the decision. Nothing here silently
overrides a decision — each such fix must land as an amendment in
[decisions.md](decisions.md) in the same pass (that file stays binding).

**Overall verdict.** The codebase is unusually disciplined for its size: hexagonal layering is
real, the PDP core is pure and property-tested, every write is audited in-transaction, migrations
are non-destructive, and the docs actually match the code far more than is normal. None of the
findings below are "the design is wrong" — they are "the design has O(n) walls that were fine at
demo scale and are load-bearing at national scale," plus a few structural debts that compound with
every new module. The authorization request path is the single biggest wall: **items R‑01…R‑04 are
one coherent problem** (authority state is recomputed and materialized app-side per request) and
should be fixed as one program of work.

---

## Severity index

| ID | Severity | Area | One-line |
|----|----------|------|----------|
| [R‑01](#r-01) | critical | scalability | Authorization state re-fetched from DB multiple times per request, no memoization |
| [R‑02](#r-02) | critical | scalability | Reach materialized app-side and shipped as comma-joined GUC strings; RLS policies linear-scan them |
| [R‑03](#r-03) | critical | scalability | Every authenticated request pins a pooled connection for its full duration (incl. external HTTP) |
| [R‑04](#r-04) | critical | scalability | Full closure DELETE + rebuild of the whole graph inside every edge-change transaction |
| [R‑05](#r-05) | high | scalability | Import path: whole dataset in one POST, one transaction, row-by-row upserts, no HTTP timeout |
| [R‑06](#r-06) | high | scalability | Person search is un-indexed `ILIKE '%q%'`; scoped list filters in Go after pagination |
| [R‑07](#r-07) | high | scalability | `audit_log`: one unpartitioned table, 6 secondary indexes, synchronous insert on every write, no retention |
| [R‑08](#r-08) | high | structure | Single sqlc config generates the whole schema into all 17 module packages (~50k duplicated LOC, invisible boundary violations) |
| [R‑09](#r-09) | high | structure | `person` is a god module (bio + contacts + social + relationships + physical identity + addresses + watchlists + overlays + …) |
| [R‑10](#r-10) | medium | architecture | Same-transaction event bus is load-bearing for 10+ modules; "extraction-ready" claim no longer holds; no outbox |
| [R‑11](#r-11) | medium | architecture | Late-binding setter web in the composition root (nil-at-request-time failure mode) |
| [R‑12](#r-12) | medium | architecture | Synchronous in-request hermenea call with no explicit deadline, made while holding the pinned DB connection |
| [R‑13](#r-13) | medium | scalability | Multi-replica story — Phase‑3 parts SOLVED here; remainder **carried to [review-2026-08](review-2026-08.md#r-13)** |
| [R‑14](#r-14) | medium | product/compliance | **Carried to [review-2026-08](review-2026-08.md#r-14)** (decision-level, needs the user) |
| [R‑15](#r-15) | low | structure | `ExpectedSchemaRevision` is a hand-bumped constant |
| [R‑16](#r-16) | low | structure | Secrets read via scattered `os.Getenv` in `main.go`, bypassing install config |
| [R‑17](#r-17) | low | structure | `SELECT *` in hot sqlc queries; wide-row hydration on list paths |
| [R‑18](#r-18) | low | docs | **Carried to [review-2026-08](review-2026-08.md#r-18)** |
| [R‑19](#r-19) | low | architecture (challenge) | **Carried to [review-2026-08](review-2026-08.md#r-19)** (decision-level, needs the user) |

---

## Findings

### <a id="r-01"></a>R‑01 — Authorization state is re-fetched per check, several times per request  **[critical]**

**Evidence.**
- `internal/authorization/application/service.go` — `Decide` runs `IsActiveInstanceAdmin` +
  `ActiveGrantsForSubject` (two queries) on every call; the comment at ~line 89 states
  "Decisions are not cached across requests."
- `internal/authorization/pep/pep.go` — `RequireAny` loops calling `Enforce`, i.e. N full
  `Decide` round-trip sets for one gate.
- `internal/identityfederation/middleware/authenticator.go` (~line 140) — the authenticator
  additionally calls `RLSStateFor` (→ `EffectiveReach` → the same two queries again + closure
  expansion) on **every authenticated request**, before the handler runs its own `Require`.

So a typical guarded request pays ≥3 executions of the grants join
(`authz_role_assignments ⋈ authz_roles ⋈ authz_role_permissions`) plus closure probes — before any
business query. There is no request-scoped memoization, let alone cross-request caching.

**Why it breaks at target scale.** PDP load is per-request multiplicative. At national scale
(thousands of RPS, subjects with tens of grants), authorization becomes the dominant DB load and
its latency is serialized in front of every handler.

**Fix sketch (do together with R‑02/R‑03/R‑04 — see roadmap Phase 1).**
1. **Request-scoped authority context**: the authenticator already resolves the subject; have it
   fetch `(isAdmin, grants)` once, stash in `context` (alongside `pkg/authn`'s subject), and make
   `Decide`/`EffectiveReach`/`FilterVisibleUnits` consume it. This is a pure refactor — semantics
   are identical (state is a snapshot at request start, which is already true per-call today).
2. **Cross-request cache (second step, needs a decision amendment)**: short-TTL (1–5 s) cache of
   `(subject → grants, isAdmin)` keyed by a revocation epoch — a single `authz_epoch` counter
   bumped in the grant/revoke/role-edit transactions makes invalidation exact and cheap
   (one indexed read to validate a hit). The "a revoke takes effect on the next call" property in
   the code comment is preserved to within the TTL; record the chosen bound in `decisions.md`.

**Acceptance.** A guarded read endpoint issues exactly one grants fetch per request (verify with
`pg_stat_statements` or a pgx tracer in an integration test); PDP bench
(`internal/authorization/domain/pdp_bench_test.go`) extended with an application-level benchmark
showing decision cost independent of the number of `Require*` calls in a request.

---

### <a id="r-02"></a>R‑02 — Reach is materialized app-side and shipped as comma-joined GUC strings  **[critical]**

**Evidence.**
- `internal/authorization/domain/pdp.go` — `ReachSet` expands a subtree grant into **all
  descendant unit IDs** via `DescendantUnitIDs` and returns them as Go maps.
- `internal/platform/db/rls.go` — `setRLSGUCs` comma-joins `ReadableUnits`/`WritableUnits` into
  the `app.readable_units` / `app.writable_units` session GUCs.
- `migrations/20260601000011_rls_backstop.sql` — policies filter with
  `col = ANY (string_to_array(current_setting('app.readable_units', …), ',')::uuid[])`.
- `internal/person/application/service.go` — `ListVisiblePersons` flattens `reach.Readable` into
  a `[]string` and passes it as an array parameter to
  `ActivePersonIDsInUnits` (`internal/membership/adapters/queries/membership.sql`,
  `WHERE unit_id = ANY(@unit_ids::uuid[])`).

**Why it breaks at target scale.** A staff-level subject with a subtree grant near an org root has
reach ≈ the whole org: 10⁴–10⁵ unit RIDs. Per request that is: one closure query returning 10⁵
rows → a Go map → a multi-megabyte GUC string set over the wire → per-statement re-parse
(`string_to_array` on a huge string) → linear `= ANY` scans per row in every RLS-guarded
statement → 10⁵-element array parameters on the membership union. Memory, network, parse and plan
cost all scale with org size **per request**.

**Fix sketch.** Invert the direction: stop exporting the reach set out of the database.
1. Replace app-side reach materialization with **SQL semi-joins**: the visible-persons union
   becomes one query joining `membership_memberships` → `tenant_unit_closure` →
   `authz_role_assignments` on the subject (grants: `unit` scope matches unit directly, `subtree`
   scope matches via closure `ancestor_id = target_unit_id`). Same shape for the shadow gate.
   `ReachSet` stays only for small-cardinality callers (or is deleted).
2. Re-shape the RLS backstop (amends **D-RLSDefenseInDepth**): the GUC carries only
   `app.person_id` + `app.is_instance_admin`; policies check reach with an
   `EXISTS (… closure ⋈ assignments …)` sub-select (Postgres caches the plan; the closure PK
   `(graph_id, ancestor_id, descendant_id)` and `tenant_unit_closure_descendant_idx` make each
   probe an index hit). This keeps the backstop exact under revocation (better than today's
   snapshot-at-request-start) and drops the GUC size to O(1).
3. Add the missing reverse-direction support index if EXPLAIN shows it:
   `authz_role_assignments (subject_person_id) WHERE revoked_at IS NULL` (verify what exists in
   `migrations/20260601000007_authorization.sql` first).

**Acceptance.** No GUC ever contains a unit list; `GET /persons` for a subject whose reach is a
100k-unit subtree runs in O(page) — demonstrated by an integration test that seeds a synthetic
10⁵-unit graph (generator script, keep under `scripts/`) and asserts p95 latency and
`pg_stat_statements` row counts; RLS policies validated by the existing
`internal/platform/db/rls_integration_test.go` extended to the new policy shape.

---

### <a id="r-03"></a>R‑03 — Every authenticated request pins a pooled connection for its full duration  **[critical]**

**Evidence.** `internal/identityfederation/middleware/authenticator.go` (~line 145) calls
`db.AcquireScoped` per request; `internal/platform/db/rls.go` shows the cost: 4 × `set_config`
round trips on acquire + 4 × on release, and the connection is held in the request context until
the response is written — including across **external HTTP calls** (the watchlist screen, R‑12)
and across handler work that never touches an RLS-guarded table (only 4 tables have policies —
see `migrations/20260601000011_rls_backstop.sql`).

**Why it breaks at target scale.** Concurrent requests ≈ pinned connections. A pgx pool of, say,
50 conns caps the server at ~50 in-flight requests regardless of how little DB work they do; one
slow downstream (hermenea) or a burst of long list queries exhausts the pool for everyone. The 8
extra round trips are pure per-request tax.

**Fix sketch.**
1. If R‑02 lands, the GUC state is two tiny values — set them with **one** round trip
   (`SELECT set_config('app.person_id',$1,true), set_config('app.is_instance_admin',$2,true)`
   with `is_local=true` inside the statement's transaction, or a single batched exec).
2. Scope pinning to need: pin lazily — only when a handler actually acquires the querier for one
   of the RLS-guarded modules (`internal/platform/db/rls.go` already routes through
   `WithConn`/`ConnFromContext`; move `AcquireScoped` behind the first `querier(ctx)` call and
   release on handler return, not on response write).
3. Never hold the pinned conn across the hermenea egress (`person/application/watchlist.go`
   path): screen first or release before the outbound call.

**Acceptance.** A load test (even `hey`/`vegeta` against a read endpoint) shows in-flight request
concurrency exceeding pool size; per-request DB round-trip count for a non-RLS endpoint (e.g.
`GET /geo/countries`) drops by 8.

---

### <a id="r-04"></a>R‑04 — Closure is fully rebuilt inside every edge-change transaction  **[critical]**

**Evidence.** `internal/tenant/adapters/queries/tenant.sql` (header comment: "transitive closure
recomputed in the write transaction on every edge change"; ~line 269 `DELETE FROM
oikumenea.tenant_unit_closure WHERE graph_id = @graph_id` + `RebuildClosureForGraph` recursive
insert). Called from `Repository.RecomputeClosure`
(`internal/tenant/adapters/repository.go` ~line 300) in the caller's write tx.

**Why it breaks at target scale.** Closure size for a 10⁵-unit graph of depth ~8 is ~10⁶ rows.
Every single edge attach/detach deletes and reinserts all of them inside the user-facing write
transaction: seconds-to-minutes of latency, a giant WAL burst, table/index bloat from full
churn, and effective serialization of all structural edits on a graph (the DELETE takes a lock
footprint that conflicts with concurrent readers of the same graph's closure via the PDP —
i.e. **authorization decisions stall while anyone edits the org chart**).

**Fix sketch (amends the maintenance strategy behind D-Graphs / D-ClosureIntegrity; the
decision's *contract* — a materialized closure the PDP reads — stays).**
- Incremental maintenance, standard DAG closure algebra:
  - **attach(parent, child):** `INSERT … SELECT anc.ancestor_id, desc.descendant_id, anc.depth +
    desc.depth + 1 FROM closure anc, closure desc WHERE anc.descendant_id = parent AND
    desc.ancestor_id = child ON CONFLICT DO NOTHING` (multi-parent DAG ⇒ closure rows are a set;
    if `depth` must stay minimal, use `ON CONFLICT … DO UPDATE SET depth = LEAST(...)`, or drop
    depth-precision — audit whether anything reads `depth`; the PDP does not).
  - **detach:** the hard direction in a DAG (alternative paths). Recompute only the **affected
    sub-closure**: rows `(a,d)` where `a ∈ ancestors(parent) ∪ {parent}` and
    `d ∈ descendants(child) ∪ {child}` — delete those pairs, re-derive them from edges with one
    recursive CTE bounded to that slice. For non-root detaches this is orders of magnitude
    smaller than the whole graph.
- Keep `RebuildClosureForGraph` as the D-ClosureIntegrity repair/verify path (that decision
  explicitly wants an on-demand rebuild — it just must stop being the per-edit path).

**Acceptance.** Property test (mirror the style of
`internal/authorization/domain/pdp_property_test.go`): random edge attach/detach sequences on a
random DAG — incremental closure ≡ from-scratch recompute after every step. Bench: single edge
attach on a synthetic 50k-unit graph touches O(ancestors × descendants-of-child) rows, not O(graph).

---

### <a id="r-05"></a>R‑05 — Import path: whole dataset per POST, one transaction, row-at-a-time upserts  **[high]**

**Evidence.**
- `internal/hermenea/loader/loader.go` — one `Load` = the entire dataset as a single JSON
  envelope; `httpclient.WithHTTPTimeout(0)` with a comment acknowledging the 27k-languoid POST
  "rebuilds the closure + family_code in a single transaction — well past conjure's 60s default".
- `internal/dataimport/application/service.go` — every handler is `for _, rec := range records`
  with per-record upserts (11 such loops); no `CopyFrom`, no chunking, one tx per envelope.

**Why it breaks at target scale.** Proven ceiling so far is 35k records. National-scale reference
data (full multi-country gazetteers, consolidated sanctions, company registries) is 10⁶–10⁷
records: the envelope alone is GBs of JSON held in memory on both sides, the single transaction
holds locks and bloats for hours, and a failure at record N loses all progress (hermenea's retry
re-sends everything).

**Fix sketch.**
1. **Chunked envelopes**: extend the import contract (`api/*.conjure.yml` for dataimport) with
   `(runId, seq, isLast)`; hermenea's connector streams chunks of ~5k records; oikumenea commits
   per chunk. Idempotency already exists per record (natural-key upserts + provenance), so chunk
   replay is safe — resumability falls out for free (store last-acked `seq` on the job).
2. **Set-based apply** inside a chunk: `CopyFrom` into a temp staging table + one
   `INSERT … SELECT … ON CONFLICT` merge per target table, replacing the per-record loop for the
   high-volume object types (geo-places, language-scheme, external-organizations, sanctions).
   Low-volume types can keep the simple loop.
3. Restore a real HTTP deadline on the loader once no single request carries a whole dataset.

**Acceptance.** A 1M-record synthetic geo-places import completes with bounded memory (< a few
hundred MB RSS on both binaries), survives a kill -9 of either side mid-run and resumes, and no
transaction lives longer than ~seconds. Existing e2e (compose run) still green.

---

### <a id="r-06"></a>R‑06 — Person search: un-indexed `ILIKE '%q%'`, and Go-side filtering after pagination  **[high]**

**Evidence.**
- `internal/person/adapters/queries/person.sql` — `ListPersons` filters
  `display_name/code/given/surname ILIKE '%' || @query || '%'`; `grep pg_trgm\|GIN migrations/`
  comes back empty — no trigram or FTS index anywhere.
- `internal/person/application/service.go` — `ListVisiblePersons` applies the text query **in Go
  to the already-paginated page** (comment at ~line 375): a page may legitimately return 0
  matches while `nextPageToken` is set, so a scoped search for a rare name pages through the
  entire visible roster one page at a time.

**Why it breaks at target scale.** Leading-wildcard `ILIKE` over four columns at 1M rows is a
sequential scan per keystroke of the console's typeahead. The scoped path is worse: correctness
degrades (empty pages) and worst-case cost is O(visible-set / pageSize) round trips.

**Fix sketch.**
1. Migration: `CREATE EXTENSION IF NOT EXISTS pg_trgm;` + one GIN trigram index over a generated
   concatenated search column (or one index per column; measure), partial `WHERE deleted_at IS NULL`.
2. Fold the query into the SQL of the scoped path: after R‑02 the visible list is one SQL join —
   add the trigram predicate there, keyset cursor stays on person RID. Delete
   `matchesPersonQuery`.

**Acceptance.** `EXPLAIN (ANALYZE)` on a 1M-row seeded `person_persons` shows bitmap index scan
for a 3-char query; scoped search never returns an empty page while `hasMore` unless the true
result is empty.

---

### <a id="r-07"></a>R‑07 — `audit_log`: unpartitioned, 6 secondary indexes, unbounded, written in every tx  **[high]**

**Evidence.** `migrations/20260601000001_audit_log.sql` — 6 `CREATE INDEX` statements; no
partitioning anywhere (`grep -i partition migrations/*.sql` → no DDL hits); D-Audit mandates a
synchronous in-tx audit row for **every** write; append-only guard (`reject_mutation`) means it
only ever grows.

**Why it breaks at target scale.** The audit table becomes the largest, hottest table in the
system: 6 index maintenance operations added to every write transaction's critical path, and
years of national-scale writes with no retention/archival story make it the dominant storage,
vacuum, and backup cost. (D-Audit's *semantics* — same-tx, append-only — are right and are not
challenged; the physical layout is.)

**Fix sketch.**
1. Declarative **monthly range partitioning** on `created_at`. This edits a shipped migration or
   (better, per upgrade-safety) lands as an expand/contract migration: create partitioned
   `audit_log_v2`, dual-write via the repository for one release, backfill, swap names. Note the
   keyset cursor `(created_at DESC, id DESC)` is partition-friendly.
2. Cut indexes to what the read API actually serves (check
   `internal/audit/adapters/queries/audit.sql`): `(created_at,id)` + `(target_type,target_id)` +
   `(actor_person_id)` are probably the keepers; `actor_type`/`request_id`/`unit_id` singles are
   candidates to drop or fold into composites.
3. Retention: an operator-configured archival policy (detach + dump partitions older than N
   months). Record as a new decision (D-AuditRetention) — legal-hold requirements belong to the
   operator, so make it config, not code.

**Acceptance.** `pgbench`-style mixed write load shows reduced per-write latency vs baseline;
partitions detach/attach cleanly in a rehearsal; `atlas migrate lint` gate still passes
(expand/contract, nothing destructive in-place).

---

### <a id="r-08"></a>R‑08 — One sqlc config generates the whole schema into every module package  **[high]**

**Evidence.** Root `sqlc.yaml`: 16 codegen blocks, each with `schema: migrations` (the full
directory). Result: `internal/<module>/adapters/<m>sql/models.go` is ~2,997 lines in **17**
packages — each contains structs for every table in the system (e.g.
`OikumeneaTenantUnit` and `OikumeneaAuditLog` inside `personsql/models.go`). ~50k lines of
duplicated generated code; `git status` in memory notes a past incident of "reverted 14
non-tenant `*sql/models.go` sqlc churn" — every migration touches all modules' generated files.

**Why it matters.** (a) The hexagonal boundary is enforced socially, not mechanically: any module
can silently write a query against another module's tables and sqlc will happily generate it —
the exact bug class the architecture exists to prevent. (b) Every migration churns 17 files ×
3k lines, polluting diffs and inviting the "revert the churn by hand" failure mode already hit
once. (c) Compile-time and review cost grow with schema × modules.

**Fix sketch.** sqlc can't natively scope a schema dir per package, so pick one:
- **Option A (recommended, no codegen change):** a CI/lint script (`scripts/check-module-tables.sh`)
  that greps each module's `queries/*.sql` for `oikumenea.<prefix>_` table references and fails on
  a table outside the module's own prefix (plus an explicit, reviewed allowlist for the known
  cross-module reads, e.g. authorization → `tenant_graphs` in `ActiveGrantsForSubject`). Cheap,
  catches the real violation (queries), tolerates the generated-struct noise.
- **Option B (stronger):** split the schema per module for sqlc only — a small generator that
  slices `migrations/` tables by prefix into `internal/<m>/adapters/schema/` inputs. Kills the
  50k duplicated lines and makes cross-module queries a compile error. More moving parts; do it
  only if A proves insufficient.

**Acceptance.** CI fails on a test PR that adds a `person` query touching `oikumenea.tenant_units`;
(Option B) a migration to one module's tables regenerates only that module's package.

---

### <a id="r-09"></a>R‑09 — `person` is a god module  **[high]**

**Evidence.** `internal/person/` owns: core bio (D-PersonBio/D-PersonNamesCLDR), contacts,
social channels, relationships, citizenships/residences, physical identity + encrypted ethnicity,
addresses, institutional ties, watchlist matches + sanctions, and the M35 overlays (crypto
wallets, personality, political leaning). Sizes: `adapters/queries/person.sql` 1,270 lines →
`person.sql.go` 6,032; `adapters/repository.go` 2,994; `transport/service.go` 1,823;
`application/service.go` 1,532; `domain/person.go` 1,259. Compare: the next-largest domain module
is ~⅓ of this. Ten other modules subscribe to its events; it holds 4 late-bound seams.

**Why it matters.** Every new person-adjacent milestone (M31–M35 all landed here) grows the same
five files; merge conflicts, review blast radius, and the temptation to share repository/tx
helpers across unrelated concerns all scale with it. It is also the module where PII tiers
concentrate — the hardest code to review is in the biggest files.

**Fix sketch.** Split by data sensitivity + change cadence, keeping ONE Conjure service surface
(transport can stay thin and delegate — the API contract need not change):
- `person` (core): identity, names, bio, ranks, read-scope, merge/purge orchestration.
- `personprofile`: contacts, social channels, relationships, addresses, institutional ties.
- `personsensitive`: physical identity/ethnicity, overlays (wallets/personality/political),
  watchlist matches + sanctions — everything envelope-encrypted or `pii:special`, so the crypto
  surface and its reviewers live in one place.
Each gets its own `application`/`adapters`/`queries` package; purge/merge stays orchestrated by
core via the existing event fan-out (the split modules subscribe like external modules already
do). Do it file-move-first (pure `git mv` + package renames), no behavior change, then split the
oversized `person.sql` along the same lines.

**Acceptance.** No file in the three modules exceeds ~800 hand-written lines; `go test ./...`
green with zero query-text changes in the first PR; purge integration test
(`internal/person/overlays_integration_test.go` and friends) unchanged and green.

**Status — DONE (uncommitted, green); D-PersonModuleSplit fully delivered incl. the doc entity-section move.** The
**application-layer** split landed
per [D-PersonModuleSplit](decisions.md#d-personmodulesplit--the-person-god-module-splits-into-core--profile--sensitive-behind-one-personservice):
new `internal/personprofile` + `internal/personsensitive` (+ shared `internal/person/appkit`) behind the
one `PersonService`, with the whole `crypto.Cipher` surface now in `personsensitive`; a **shared**
`domain` kernel + **unified** `adapters`/`queries` keep merge/purge and `GetPerson` working with zero
query-text change; `go build/vet/test ./...` + full integration green. All person-family hand-written
files are < 800 lines **except** `adapters/repository.go` (3003) + `domain/person.go` (1259) — the
data/domain layer, deferred to **PR‑2b**. Docs: [personprofile.md](../modules/personprofile.md) +
[personsensitive.md](../modules/personsensitive.md).

**PR‑2b step 1 DONE (uncommitted, green).** The **`PersonPurged` + `SubscribeErase` fan-out** landed:
`PurgePerson` publishes `PersonPurged` and `education`/`company` erase their own person-referencing rows
via `SubscribePersonPurge` — **`person` no longer deletes `company_*`/`education_*` tables inline** (the
R‑08 boundary, no allowlist). Behavior-identical; 12 cross-module deletes removed from `person`'s
`repo.Purge` + `queries/person.sql` (sqlc regen); education/company purge tests re-driven through the
event; new end-to-end `TestPurgePublishesPersonPurged`.

**PR‑2b step 2 DONE (uncommitted, green).** The dormant `document`/`finance`/`vehicle`/`religion`
`ErasePerson*` methods are now wired as `PersonPurged` subscribers (`SubscribePersonPurge` → extracted
`tx`-accepting helpers, subscribing to `TypePersonPurged` directly since they crypto-erase + audit) —
**closing a real purge PII gap** (before, a `PurgePerson` left those modules' rows un-erased; the erase
methods existed but nothing called them on purge). Audit row recorded only when something was erased; new
`TestPersonPurgeEventErases` (document) proves the subscriber end-to-end.

**PR‑2b step 3 DONE (the data-layer split; uncommitted, green).** `queries/person.sql` (182 queries) +
`adapters/repository.go` (~3000 lines) split three ways by owning table: each concern now has its **own
`adapters/queries` dir + generated sqlc package** (`personprofilesql`/`personsensitivesql`) and its **own
repository port** (`Repository`/`ProfileRepository`/`SensitiveRepository`) in the shared domain kernel,
implemented by its own adapter. `domain.Person` is **lean** (seven profile child-slices dropped) and the
transport `GetPerson` composes them from the profile service (`composeProfile`); core `repo.Purge` is
**core-only**, with profile/sensitive erasing their own rows via `SubscribePersonPurge` in the purge tx
(and `MergePerson` publishing `PersonPurged` to drop the tombstoned stub's residuals). Cross-concern reads
are reviewed: a `PersonExists` parent guard on `person_persons`, and a late-bound `PEPStatusReader` seam
(sensitive → profile) so watchlist screening never touches `person_government_positions`. The **R‑08 lint
gained per-`person_*`-table ownership** (exact names override the `person` prefix) + reviewed allowlist
entries. Every person-family file is now **< 800 lines** except three (re-measured 2026-07-09): the two
concern adapters (`personprofile/adapters/repository.go` 1,761; `personsensitive/adapters/repository.go`
1,180) and the shared domain kernel `person/domain/person.go` (1,291) — an accepted residual, not a
boundary problem. Full
`go test -tags integration ./...` = **54 packages ok**; server boots with the seam assertions passing.
**Docs entity-section move DONE (2026-07-09):** the `person_*` data-model sections moved from person.md
into [personprofile.md](../modules/personprofile.md) / [personsensitive.md](../modules/personsensitive.md)
(each entity has exactly one owning doc; person.md keeps core + the read-scope rule + the one
`PersonService` API surface). **R-09 fully resolved.**

---

### <a id="r-10"></a>R‑10 — Same-tx event bus is load-bearing everywhere; extraction-readiness is now aspirational  **[medium]**

**Evidence.** `pkg/events/events.go` (synchronous, same-transaction, handlers receive the
publisher's `pgx.Tx`); `cmd/oikumenea/main.go` wires `SubscribePersonEvents` from **ten** modules
(membership, order, authorization, document, education, company, vehicle, finance, religion,
identityfederation). The docs' extraction story ("cross-module mutations are domain events, keeps
the monolith extraction-ready") assumes events could become async; but merge/purge correctness
now *depends* on same-tx atomicity across ten modules' tables, and the outbox seam is explicitly
"a later addition" (package comment).

**Why it matters at target scale.** (a) A person merge/purge is one giant transaction spanning
10+ modules' tables — long lock lists, and every new module widens it. (b) Any real extraction
(or even a second binary sharing the DB) silently breaks the atomicity contract. (c) There is no
at-least-once channel for effects that *should* be async (notifications, projections, cache
invalidation for R‑01's epoch), so everything defaults into the tx.

**Fix sketch.** Not "make it async" — same-tx is the correct semantic for order-apply and
merge re-homing. Instead:
1. **Classify** events in `docs/architecture/patterns.md`: `atomic` (same-tx, current bus) vs
   `notify` (after-commit). Today's events all stay `atomic`.
2. Implement the parked **transactional outbox** for the `notify` class (one table, one poller;
   hermenea's job-queue code is a working template — `internal/hermenea/runtime`), so the next
   async-shaped need (R‑01's epoch bump, webhooks, search-index refresh) doesn't get jammed into
   the write tx by default.
3. Add a guardrail: the bus refuses `Subscribe` after boot (it already assumes this; enforce it)
   and the docs state that adding an `atomic` subscriber is a decision-level change (it widens
   every publisher's transaction).

**Acceptance.** Outbox exists with an integration test (publish-after-commit, crash between
commit and dispatch → redelivered); patterns.md documents the two classes; no behavior change to
existing events.

**Status — DONE (uncommitted, green); D-EventOutbox.** All three parts landed. (1) **Classified** —
[patterns.md](patterns.md) *Domain events: atomic vs. notify* + [decisions.md](decisions.md) D-EventOutbox;
`atomic` stays the default and every event today is `atomic`. (2) **Transactional outbox** — new
`oikumenea.platform_outbox` (migration `0036`; infra, no RLS); producer side `pkg/events.OutboxWriter`
enqueues on the write tx; the `internal/platform/outbox` **dispatcher** drains after commit, claiming
`FOR UPDATE SKIP LOCKED` (replica-safe, mirrors the hermenea worker), backoff + dead-letter, at-least-once
(idempotent handlers). Started in `main.go`, folded into cleanup. **No `notify` producers yet** — a
live-but-empty proven seam. (3) **Guardrail** — `events.Bus.Seal` is called after all subscribers wire;
a later `Subscribe` **panics** (unit-tested), and the docs state an `atomic` subscriber is a
decision-level change. Verified: `internal/platform/outbox/outbox_integration_test.go` (enqueue-on-commit
durability across the commit/dispatch gap → delivery → at-least-once redelivery on handler failure →
dead-letter) + `pkg/events` Seal unit tests; full `go test -tags integration ./...` = **55 packages ok**;
server boots clean (dispatcher started, bus sealed, no seam panic). No behavior change to existing events.

---

### <a id="r-11"></a>R‑11 — Late-binding setter web in the composition root  **[medium]**

**Evidence.** `cmd/oikumenea/main.go`: `pep.NewUnbound()` → `Bind` inside
`authorization.Register`; `middleware.NewUnbound()` → `authenticator.Bind(...)` near the end;
`personSvc.SetMembershipReader(membershipSvc)`, `personSvc.SetLocationLookup(geoSvc)`,
`personSvc.SetWatchlistLookup(...)` (conditional on config!), plus guards like
`if s.membership == nil` scattered in `person/application` (`ReadablePerson`,
`ListVisiblePersons`).

**Why it matters.** The wiring contract is temporal and implicit: forgetting one `Set*`/`Bind`
compiles fine and fails at request time (or worse — `ListVisiblePersons` silently returns an
empty page when `s.membership == nil`, which reads as "no access" rather than "mis-wired
server"). Each new cross-module seam has copied the pattern, so the root grows more
order-sensitive with every milestone.

**Fix sketch.** Keep the pattern (it resolves real construction cycles) but make it fail-fast and
uniform: give every late-bound holder a `MustBeBound() error`, collect them in a
`[]interface{ MustBeBound() error }` in `initServer`, and check the whole list right before
returning from the InitFunc — one place, boot-time panic instead of request-time nil/empty
behavior. Make the optional watchlist seam an explicit no-op implementation
(`watchlistclient.Disabled{}` returning the "not configured" error) rather than a nil check.

**Acceptance.** Deleting any `Set*`/`Bind` line makes the server refuse to boot with a message
naming the missing seam; no `== nil` seam guards remain in application code.

**Status — DONE (uncommitted, green).** Every mandatory late-bound holder now exposes
`MustBeBound() error`; the composition root collects them in one
`[]interface{ MustBeBound() error }{authenticator, enforcer, personSvc, profileSvc, sensitiveSvc}`
(`cmd/oikumenea/main.go`) and checks the whole list before the InitFunc returns — a missing seam is a
boot failure naming it, not a request-time nil/empty page. Covered by
`internal/person/application/readscope_test.go` `TestMustBeBound`. (The R‑09 split added the profile/
sensitive services to the same slice.)

---

### <a id="r-12"></a>R‑12 — In-request synchronous hermenea call: no deadline, made while holding the pinned connection  **[medium]**

**Evidence.** `cmd/oikumenea/main.go` (~line 282) builds the watchlist client with
`WithMaxRetries(0)` but no `WithHTTPTimeout`; `internal/watchlistclient/client.go` adds none. The
call happens inside a request whose context carries the pinned RLS connection (R‑03). Hermenea in
turn calls INTERPOL/OFAC upstreams (≤24h cache, but a cache miss means live egress).

**Why it matters.** oikumenea's request latency (and a pooled DB connection) is coupled to a
third-party government API's tail latency. A hung upstream turns into pool exhaustion in
oikumenea (the fate-sharing R‑03 makes global).

**Fix sketch.** `httpclient.WithHTTPTimeout(10 * time.Second)` (hermenea serves cached answers in
ms; a miss that needs >10s should fail into the existing "screening unavailable" error path);
release/avoid the pinned conn around the egress (part of R‑03 item 3). Optionally a trivial
half-open breaker later; the timeout is the load-bearing part.

**Acceptance.** Integration test with a stalling fake hermenea: request fails in ~10s with the
degraded-screening error; concurrent unrelated requests unaffected (pool not exhausted).

---

### <a id="r-13"></a>R‑13 — Multi-replica story  **[medium — Phase‑3 parts SOLVED; remainder carried]**

The Phase‑3 parts are **solved and verified here** (see the M49 resolution-log entry): advisory-locked
boot seeding (`db.LockBootSeed`, one seed pass proven under `--scale app=2`), hermenea
`worker.concurrency` N claim loops over the SKIP LOCKED queue (4-way parallelism proven), and
idempotent-per-window scheduler enqueue (`cron:<source>:<window>` idempotency key +
`ON CONFLICT DO NOTHING` — re-verified 2026-07-09 in
`internal/hermenea/application/service.go` `EnqueueDueSchedules`).

**The remainder — the multi-replica `notify`/outbox posture, the two-replica compose verification,
and the `overview.md` multi-replica write-up — is carried to
[review-2026-08.md § R‑13](review-2026-08.md#r-13).**

---

### <a id="r-14"></a>R‑14 — Product-scope creep with concrete compliance liabilities  **[medium, decision-level — CARRIED]**

Open, decision-level (needs the user). **Carried unchanged to
[review-2026-08.md § R‑14](review-2026-08.md#r-14).**

---

### <a id="r-15"></a>R‑15 — `ExpectedSchemaRevision` is a hand-bumped constant  **[low]**

**Evidence.** `internal/platform/db/schemaversion.go`: `const ExpectedSchemaRevision =
"0035_person_overlays"`. Project memory already records a shipped bug from forgetting the bump
(M32 gotcha: readiness gate failed until the const was updated).

**Fix.** `//go:embed` the repo's `migrations/atlas.sum` into the binary and derive the expected
revision from its last entry at init. One function, deletes the footgun class. (The hermenea
binary should do the same against `migrations/hermenea/atlas.sum` if it has an equivalent gate.)

**Acceptance.** Adding migration 0036 with no Go change leaves the boot gate correct;
the const is gone.

**Status — DONE (uncommitted, green).** `ExpectedSchemaRevision` is now
`var ExpectedSchemaRevision = mustDeriveExpectedRevision()` — derived at init from the `//go:embed`ed
`migrations.FS` (scan files in version order, take the last `schema_version` revision literal;
`internal/platform/db/schemaversion.go`). The hand-bumped const and its footgun are gone; adding a
migration needs no Go change.

---

### <a id="r-16"></a>R‑16 — Secrets via scattered `os.Getenv` in `main.go`  **[low]**

**Evidence.** `cmd/oikumenea/main.go`: `os.Getenv("OIKUMENEA_HERMENEA_TOKEN")` (twice, two call
sites that could drift) and `os.Getenv("HERMENEA_OIKUMENEA_TOKEN")`, while everything else flows
through the install/runtime config (ECV exists exactly for secret values). Two near-identical
names in two directions is also an operator-error magnet.

**Fix.** Move both into `config.Install`/runtime (refreshable, ECV-encryptable), read env only as
a documented override in one place (`config` package), and rename for direction clarity
(`hermenea.outbound-token` / `hermenea.inbound-token`). Update
`docs/modules/hermenea.md` + compose files in the same pass.

**Status — DONE (uncommitted, green).** Both tokens now live in install config as
`config.Hermenea.OutboundToken` / `InboundToken` (`hermenea.outbound-token` / `hermenea.inbound-token`);
`main.go` reads them via `install.Hermenea.ResolveOutboundToken()` / `ResolveInboundToken()`, which
honour the `OIKUMENEA_HERMENEA_TOKEN` / `HERMENEA_OIKUMENEA_TOKEN` env vars as a single documented
override each. No `os.Getenv` for secrets remains in `main.go`.

---

### <a id="r-17"></a>R‑17 — `SELECT *` on hot list paths  **[low]**

**Evidence.** 39 × `SELECT *` in `internal/person/adapters/queries/person.sql` alone (education
40, company 25, …). `person_persons` is a wide row (names, attributes jsonb, …); list endpoints
hydrate all of it to render a directory row.

**Fix.** Not a blanket rewrite: after R‑02/R‑06 land, give the two genuinely hot queries
(`ListPersons`, the scoped-list hydration) explicit column lists (sqlc generates a lean row
struct). Leave single-row gets as `*`.

**Status — DONE (uncommitted, green).** The three hot directory-list queries in
`internal/person/adapters/queries/person.sql` (`ListPersons`, the per-keystroke `SearchPersons`, and
the scoped-list hydration `ListPersonsByIDs`) now carry an **explicit column list** that omits the two
columns the row→domain mapper never reads: the wide generated `search_text` trigram haystack (added
by R‑06/M50) and the always-NULL-in-these-paths `deleted_at`. `attributes` is kept (list responses
return it — behaviour-preserving). sqlc emits three field-identical lean row structs; because their
underlying types are identical they convert to `ListPersonsRow` and map through **one** `leanToPerson`
mapper (the full-row `toPerson` + `SELECT *` stay for single-row gets: `GetPerson`,
`GetActivePersonByCode`). No schema change, no API change; person list/search/scoped integration tests
green (full `go test -tags integration ./...` = 54 packages ok), module-table lint green.

---

### <a id="r-18"></a>R‑18 — CLAUDE.md / docs drift  **[low — CARRIED]**

Open (evidence re-verified 2026-07-09: CLAUDE.md still says "implemented through M15" / "Eleven
modules"). **Carried with updated evidence to
[review-2026-08.md § R‑18](review-2026-08.md#r-18).**

---

### <a id="r-19"></a>R‑19 — D-i18n "all locales in every response" is uncapped  **[low, decision challenge — CARRIED]**

Open, decision-level (needs the user). **Carried unchanged to
[review-2026-08.md § R‑19](review-2026-08.md#r-19).**

---

## Roadmap

Phases are ordered by (risk removed ÷ effort) and by dependency. Each phase is sized to be a
milestone in the existing pipeline (`docs/development-process.md`); assign M-numbers when
adopted. **Do not start Phase 1 without Phase 0's harness — every Phase 1 item's acceptance
criterion is a measurement.**

### Phase 0 — Measurement harness (prereq, small)
- Synthetic-scale seed script (`scripts/seed-scale.sh` or Go generator): 10⁵ units in one graph,
  10⁶ persons, memberships, a realistic grant mix. Behind an env gate like the existing
  `OIKUMENEA_LANG_E2E` pattern.
- Enable `pg_stat_statements` in the dev/compose Postgres; add a tiny latency/round-trip
  assertion helper for integration tests (pgx tracer counting queries per request).
- Baseline numbers recorded in this file (append a "Measurements" section).

### Phase 1 — The authorization wall (R‑01, R‑02, R‑03, R‑12) **[critical path]**
One program of work, sequenced: request-scoped authority context (R‑01.1) → SQL semi-join reach
(R‑02.1) → RLS policy reshape + O(1) GUCs (R‑02.2, amends D-RLSDefenseInDepth in decisions.md) →
lazy/cheap connection pinning (R‑03) → watchlist deadline + egress outside pinned conn (R‑12).
Cross-request grant cache (R‑01.2) only if Phase 0 numbers still demand it afterwards.

### Phase 2 — Closure incremental maintenance (R‑04) **[critical, independent of Phase 1]**
Attach/detach incremental algebra + property test vs full rebuild; keep rebuild as the
D-ClosureIntegrity repair path. Amend the maintenance note in decisions.md (D-Graphs).

### Phase 3 — Import pipeline industrialization (R‑05, parts of R‑13)
Chunked envelopes (Conjure contract change + hermenea loader + dataimport apply loop),
CopyFrom+merge for the 4 high-volume object types, real HTTP deadline, hermenea worker
concurrency, advisory-locked boot seeding. Prove with a 1M-record synthetic run in compose.

### Phase 4 — Search & audit physicals (R‑06, R‑07)
pg_trgm GIN + SQL-side scoped search; audit_log partitioning via expand/contract + index diet +
D-AuditRetention decision. Both are self-contained migrations + query changes.

### Phase 5 — Structure debt (R‑08, R‑09, R‑11, R‑15, R‑16, R‑17) **[DONE — uncommitted, green]**
Module-table lint (R‑08 option A) first — it protects the R‑09 split. Then the person module
3-way split (move-only PR, then query-file split), boot-time seam assertion, embedded schema
revision, secret config consolidation, hot-path column lists. No schema changes in this phase.
**All six landed (uncommitted on `main`, full `go test -tags integration ./...` = 54 packages ok):**
R‑08 (module-table lint + per-`person_*`-table ownership), R‑09 (person → core/personprofile/
personsensitive behind one `PersonService`, D-PersonModuleSplit — fully delivered incl. the docs
entity-section move), R‑11 (uniform `MustBeBound` boot-time seam assertion), R‑15 (`//go:embed`-derived schema
revision), R‑16 (hermenea tokens in install config), R‑17 (lean explicit-column projection on the
three hot person list/search queries). See each finding's **Status** line.

### Phase 6 — Eventing seam & scale-out (R‑10, rest of R‑13) **[R‑10 DONE; scale-out remainder CARRIED]**
Outbox for the `notify` event class + event classification in patterns.md; two-replica compose
verification; document the multi-replica posture in `overview.md`. **R‑10 landed** (D-EventOutbox:
`platform_outbox` + dispatcher + bus `Seal` guardrail; migration `0036`; uncommitted, 55 pkgs ok — see
the R‑10 Status). The rest of R‑13 is carried to
[review-2026-08.md](review-2026-08.md#r-13) (its Phase 8).

### Phase 7 — Decisions & docs (R‑14, R‑18, R‑19) **[CARRIED]**
Carried whole to [review-2026-08.md](review-2026-08.md) (its Phase 7, unchanged scope): D-DataScope
(PAN policy — truncate+blind-index recommended), sensitive-read permission audit, CLAUDE.md refresh,
decision index tables, i18n locale-cap note. Cheap, but decision-level: needs the user, not just an
agent.

### Explicitly NOT recommended
- Replacing the custom PDP with OpenFGA/SpiceDB — evaluated and deferred 2026-07-03 (see project
  memory / authorization.md revisit-triggers); Phase 1 addresses the actual bottleneck, which is
  I/O shape, not decision logic.
- Splitting the monolith into services — the module seams are the problem to *strengthen*
  (Phases 5–6); process boundaries would multiply the R‑10 problem, not solve it.
- Postgres sharding / read replicas — nothing in the findings is beyond one well-indexed primary
  once Phases 1–4 land; revisit only with Phase 0 measurements in hand.

## Measurements

Harness: `scripts/seed-scale` world (100,000 units in one authority-bearing graph — random
recursive DAG, 1,196,989 closure rows; 1,000,000 persons with one active membership each; three
probe subjects) + `TestScaleMeasure` (`internal/authorization/scale_measure_integration_test.go`)
with the `db.WithQueryCounter` pgx tracer. Dev machine, local Postgres 16 (postgis image),
pool via `db.NewPool`.

### Baseline — 2026-07-05, before Phase 1 (commit state: M45 uncommitted on `main`)

| Metric (per request unless noted) | root subject (reach 100k units) | mid (658) | leaf (1) |
|---|---|---|---|
| `EffectiveReach` (app-side reach materialization) | **2.67 s** | 3 ms | ~0 |
| `RLSStateFor` → GUC payload | **7,399,998 bytes** | 48,690 B | 72 B |
| `AcquireScoped`+release `set_config` statements | 4 + 4 | 4 + 4 | 4 + 4 |
| Guarded request (`RLSStateFor` + 3×`Enforce`): wall | **2.65 s** | 3 ms | ~0 |
| … total statements | **112** (closure paged ×100) | 16 | 8 |
| … grants-join executions (`authz_role_assignments`) | 4 | 4 | 4 |
| … instance-admin probes | 4 | 4 | 4 |
| Visible-persons page (`ANY(reach array)`, LIMIT 51) | **1.46 s** | 27 ms | ~0 |

Reading: every R-01/R-02/R-03 claim reproduces — per-request cost scales with org size (reach
expansion + multi-MB GUC + array-parameter page), and the grants join runs once per gate instead
of once per request.

### After — 2026-07-06, Phase 1 (M47) landed

Same harness, same probe subjects, re-seeded world (`TestScaleMeasure` post-refactor shape).

| Metric (per request unless noted) | root (reach 100k) | mid (658) | leaf (1) | baseline (root) |
|---|---|---|---|---|
| Authority resolution (`ContextWithAuthority`) | 3 ms | 1 ms | ~0 | 2.67 s (reach expansion) |
| GUC payload | **40 B** | 40 B | 40 B | 7,399,998 B |
| `set_config` statements per acquire | **1** (+1 reset) | 1 | 1 | 4 + 4 |
| Guarded request (auth + 3×gates): wall | **~0 ms** | ~1 ms | ~0 | 2.65 s |
| … grants-join executions | **0–1** (1 cold, 0 within cache TTL) | 0–1 | 0–1 | 4 |
| Visible-persons page (SQL semi-join, LIMIT 51) | **170 ms cold / 39 ms warm** (dense shape) | 54 ms | 3 ms | 1.46 s |
| RLS-guarded unit page (50 rows, live policy) | 3.2 ms | — | — | n/a (policy read a 7.4 MB GUC) |

### After — 2026-07-06, Phase 2 (M48) landed

Same seeded world (100k units, 1,196,989 closure rows), `TestScaleClosureMeasure`
(`internal/tenant/scale_closure_integration_test.go`): single edge edits through the service
(audit row included), closure-row delta = rows actually touched.

| Operation (one edge edit, in-tx) | wall | closure rows touched | pre-M48 cost (full rebuild) |
|---|---|---|---|
| Attach fresh leaf, mid-tree (subtree 658) | 10 ms | 5 | **16.6 s** / 1.2M rows |
| Detach fresh leaf, mid-tree | 8 ms | 5 | 16.6 s / 1.2M rows |
| Attach fresh leaf, near-root (subtree 49,242) | 3 ms | 3 | 16.6 s / 1.2M rows |
| Detach fresh leaf, near-root | 42 ms | 3 | 16.6 s / 1.2M rows |
| Detach existing mid-tree edge (moves 658-unit subtree) | 265 ms | 1,914 | 16.6 s / 1.2M rows |
| Re-attach same edge | 177 ms | 1,914 | 16.6 s / 1.2M rows |

Reading: R‑04's claim reproduced (the timed full `RebuildClosureForGraph` of the 100k graph —
what every edge edit paid before — is 16.6 s of DELETE+reinsert of all 1.2M rows); after, an edge
edit touches only the affected `anc*(parent) × desc*(child)` slice, and cost is proportional to
that slice, not the graph.

### After — 2026-07-07, Phase 3 (M49) landed

Harness: 1,000,000-place synthetic WOF gazetteer (`scripts/gen-wof-synthetic` — 1 country + 30
regions + 3,000 counties + 996,969 localities, real hierarchy + point geometry) served to the real
compose stack through the real `wof-sqlite` connector (`wof-geo-synth` source), loaded as a chunked
run (5,000 records/chunk → 204 chunks, one oikumenea transaction each).

| Metric (1M-record run) | value | pre-M49 posture |
|---|---|---|
| End-to-end wall (incl. two kill −9 + backoffs) | **4 m 31 s** | proven ceiling 35k records; 27k = one >60 s POST |
| Merge transaction (5k records) | **320 ms mean / 616 ms max** | ONE transaction for the whole dataset |
| kill −9 hermenea mid-run (seq 56) | resumed from cursor, attempt 2 | restart from record 0 |
| kill −9 oikumenea mid-run (seq 126) | chunk failed → backoff → resumed, attempt 4 | whole run lost |
| Chunk replay after both kills (audit `runId`/`seq`) | **0 duplicates** (204 distinct seqs, once each) | n/a |
| RSS at full throughput | oikumenea **41 MiB**, hermenea **87 MiB** | envelope alone = whole dataset in memory, both sides |
| Idempotent re-run | 1,000,000 skipped in **72 s** | n/a |
| 27k Glottolog full-load e2e (merge path) | 27,127 created + 50 updated; closure 212,955; green | green (row-at-a-time) |
| `--scale app=2` fresh-DB boot | one pinax pass (8 presets, 27,177 languoids), replica 2 waits → "bootstrap skipped"; 0 constraint errors | both replicas would race the seed |

Resolution log:
- **Phase 0 → M46 (verified)**: `scripts/seed-scale`, `db.WithQueryCounter` tracer,
  `pg_stat_statements` in compose, this section.
- **Phase 1 → M47 (verified)**: R‑01 resolved (D-AuthzRequestContext + D-AuthzGrantCache: snapshot
  per request, epoch-validated 2 s cache, cross-replica convergence test); R‑02 resolved
  (D-RLSLiveReach: SQL semi-join reach with adaptive sparse/dense plan shapes, live RLS predicate
  `authz_unit_in_reach`, O(1) GUCs; randomized Go⇄SQL⇄policy differential test); R‑03 resolved
  (lazy pinning — a request that never touches an RLS-consuming module holds no pooled connection;
  in-flight > pool size proven in `lazyconn_integration_test.go`); R‑12 resolved (10 s
  `WithHTTPTimeout` on the watchlist client + stall test proving no DB connection is held across
  the egress). Bonus posture upgrade: the RLS backstop is now exact under revocation.
- **Phase 2 → M48 (verified)**: R‑04 resolved (amends the D-Graphs maintenance note in
  decisions.md): attach = `anc*(parent) × desc*(child)` closure∘closure join with `LEAST`-depth
  merge + reflexive seeding; detach = slice delete + bounded re-derivation (edge-walk inside the
  ancestor set + one trusted closure jump) + reflexive prune, gated on the edge having existed;
  per-graph `FOR NO KEY UPDATE` lock serializes closure maintenance and closes the
  guard-then-insert cycle race; `RebuildClosureForGraph` survives only as the D-ClosureIntegrity
  repair path, and `VerifyClosureForGraph` is now depth-inclusive. Proven by a random
  attach/detach differential test against an independent Go BFS oracle
  (`internal/tenant/closure_property_integration_test.go`) + the scale numbers above.
- **Phase 3 → M49 (verified)**: R‑05 resolved (amends **D-Hermenea** in roadmap-decisions.md):
  chunked envelopes (`runId`/`seq`/`isLast`, optional — single-shot semantics preserved for small
  catalogs/pinax/CLI), ~5k-record chunks each their own oikumenea transaction with a trailing
  finalize chunk for the batch finalizers; the 4 high-volume upsert handlers apply each chunk with
  ONE parallel-array UNNEST merge (`ON CONFLICT … WHERE source_version IS DISTINCT FROM EXCLUDED`,
  `RETURNING (xmax = 0)` for exact counts; two-pass self-referential parent resolution — a
  deliberate departure from this finding's CopyFrom+temp-staging sketch, keeping the merges typed
  and sqlc-generated at identical chunk-scale throughput); checksum-guarded resume cursor on the
  hermenea job (`resume_seq`/`resume_checksum`, additive migration `migrations/hermenea/0005`);
  the loader's `WithHTTPTimeout(0)` escape hatch retired (120 s default per chunk request).
  Proven by a geo-merge differential test vs an in-Go oracle, loader chunk/resume unit tests, the
  27k Glottolog e2e on the merge path, and the 1M-record compose run above (kill −9 both sides,
  zero chunk replays). R‑13 **partially** resolved (its Phase‑3 parts): `worker.concurrency` N
  claim loops over the already-safe SKIP LOCKED queue (4-way parallelism proven by a barrier
  integration test) + advisory-locked boot seeding (`db.LockBootSeed`; mutual-exclusion test +
  the `--scale app=2` one-seed-pass run above; `docker-compose.scale.yml` un-publishes host
  ports). The multi-replica eventing/notify posture (rest of R‑13) stays with Phase 6.

### After — 2026-07-08, Phase 4 (M50) landed

Search & audit physicals. Both are self-contained migrations + query changes (edited in place,
pre-release DB rebuild). Person plans measured on a 200k-row seeded `person_persons` (+ ANALYZE);
audit routing/retention verified on the real partitioned table.

| Metric | value | pre-M50 posture |
|---|---|---|
| Admin `SearchPersons`, rare 3-char term, custom plan (pgx) | **0.35 ms**, GIN bitmap index scan | `ILIKE '%q%'` seq scan, **183 ms** at 200k / linear at 1M |
| Same, forced **generic** plan (worst case) | **4.3 ms** (inner UNION still index-served) | 60 ms for the naive `A OR EXISTS(subquery)` shape (non-indexable) |
| Scoped `VisiblePersonIDsForSubjectSearch` | trigram-led GIN bitmap + per-candidate reach probe | Go-side filter on the paginated page (empty-page-while-`hasMore` bug) |
| Variant/alias find (native-script + aka) | found by admin + scoped search | unfindable (search ignored `person_name_variants`) |
| `audit_log` write path | monthly RANGE partition; **4** secondary indexes + PK `(id,created_at)` | one unpartitioned table, **6** secondary indexes, unbounded |
| Audit partition routing | Jul row→`audit_log_y2026m07`, gap row→`audit_log_default`; UPDATE/DELETE rejected on partitions | n/a |
| Retention | `detach_audit_partitions_before('2026-08-01')` detaches July cleanly | mass `DELETE` over the biggest table |

Resolution log:
- **Phase 4 → M50 (verified)**: R‑06 resolved (**D-PersonSearch**): `pg_trgm` GIN over `STORED`
  `search_text` on `person_persons` + `person_name_variants`; admin `SearchPersons` and scoped
  `VisiblePersonIDsForSubjectSearch` are UNION-of-id-set semi-joins (each branch index-served — an
  `A OR EXISTS(subquery)` predicate is not), keyset on person RID; the Go `matchesPersonQuery`
  post-filter is deleted (fixes the empty-page-while-`hasMore` scoped bug); variant/alias now
  searchable. Proven by `internal/person/search_integration_test.go` (admin variant/alias find;
  scoped no-empty-page drain + no out-of-reach leak) + the plans above. R‑07 resolved
  (**D-AuditRetention**, amends the physical layout of **D-Audit**): `audit_log` monthly
  RANGE-partitioned by `created_at`, PK `(id,created_at)`, index diet (6→4), boot-time
  `ensure_audit_partition` roll-forward under the boot-seed advisory lock + `DEFAULT` backstop,
  operator `detach_audit_partitions_before` + `audit.retention-months` config (default retain
  forever). Semantics unchanged: `reject_mutation` + RLS cascade to partitions (proven — UPDATE/DELETE
  rejected on a partition; `internal/platform/db` RLS integration test green on the partitioned
  table). Automated scheduled retention enforcement stays an open seam.

### M56 ticket 2 — person facet filters (2026-07-28)

Same harness, extended: `scripts/seed-scale -enrich` gives every facet column a realistic
distribution (49/48/2/1% sex, 96/3/1% status, 40% unknown birthdate over a 60-year span, long-tail
country, ranks for 60%, accounts for 35%, unit level/kind/visibility/state/pdpScoped). **Without it
the measurement is meaningless** — every facet predicate was 100%- or 0%-selective, because the
world only ever populated the columns the authorization path reads.

Measured through the real Go service (`TestFacetScale*`,
`internal/person/facet_scale_integration_test.go`), first page of 50 at 10⁶ persons / 10⁵ units:

| Filter | admin | leaf (reach 1) | mid (658) | root (100k) |
|---|---|---|---|---|
| unfiltered | 3.5 ms | 6.4 ms | 64.8 ms | 178.2 ms |
| `sex` | 1.1 ms | 3.1 ms | 45.5 ms | 172.3 ms |
| `status` (1% selective) | 5.8 ms | 3.0 ms | 31.4 ms | 175.1 ms |
| `birthdate` range | 2.2 ms | 2.5 ms | 31.7 ms | 199.4 ms |
| `countryOfBirth` | 0.5 ms | 1.7 ms | 53.8 ms | 197.1 ms |
| `rankId` | 43.0 ms | 2.3 ms | 65.5 ms | 222.2 ms |
| `hasAccount` | 2.0 ms | 3.7 ms | 59.6 ms | 205.5 ms |
| `unitId` (subtree) | 46.4 ms | 6.0 ms | 133.8 ms | 274.6 ms |
| `sex` + `unitId` | 73.0 ms | 2.5 ms | 95.7 ms | 316.7 ms |

**No path sequential-scans `person_persons`** — asserted, not merely observed, by
`TestFacetScaleNoSeqScan`, which EXPLAINs each shape and fails on a `Seq Scan`. That is the R-21
failure mode, and a wall-clock budget alone would not catch it: a warm cache makes a seq scan look
survivable at 10⁶ and catastrophic at 10⁷.

Three findings changed the code:

1. **The correlated closure probe was the `unitId` cost.** Expanding the subtree per candidate
   person measured **242 ms**; hoisting it to an uncorrelated `unit_id IN (SELECT … UNION SELECT
   descendants)` — a subquery that reads only the facet parameters, so the planner evaluates it once
   and probes a hash — measured **33 ms**, and held at 38 ms under `plan_cache_mode =
   force_generic_plan` (the generic-plan case R-21 warns about) against 440 ms for the correlated
   form.
2. **`VisiblePersonIDsForSubjectDense` is rewritten to drive from `person_persons`** (the shape
   `…Search` already used), so the facet predicates sit on the driving relation: `unitId` at root
   reach went **816 ms → 30 ms**, and the unfiltered baseline improved from the recorded 39 ms to
   1.4 ms because the shape terminates early instead of walking every in-reach membership. Sparse
   was rewritten to match; the two candidate shapes measured within noise of each other there
   (38 vs 42 ms at mid reach), so consistency decided it — all three visibility queries now have one
   structure and one copy of the facet block, and the `DISTINCT` + sort is gone.
3. **The harness was measuring its own bloat.** The enrichment rewrites every person row several
   times, leaving a 749 MB heap for 10⁶ persons against 150 MB reclaimed. On the bloated table the
   two candidate sparse shapes looked 2× apart; on the clean one they are equivalent. `-enrich` now
   ends with `VACUUM (FULL, ANALYZE)` so this cannot mislead the next measurement.

A fourth finding came from the **live** end-to-end run, not from any test:

4. **The `unitId` facet returned an EMPTY page to an instance admin.** person's own tables carry no
   row security, so the directory list had always read from the bare pool; the facet's predicate
   probes `membership_memberships`, which **is** RLS-protected. On a connection that is not the
   request's pinned one the `app.*` GUCs are unset, so `authz_unit_in_reach` sees neither the
   instance-admin bypass nor any grant, the `EXISTS` matches nothing, and the endpoint answers `200`
   with zero rows to a caller entitled to every one of them — the worst shape of wrong, because it
   reads as "no results" rather than as a fault. Fixed by routing the two list paths through
   `db.RequestQuerier(ctx, s.pool)`, as every other module with RLS-touching reads already does.
   **The integration suite could not have caught this**: it connects as a superuser and so bypasses
   RLS entirely. The regression guard is therefore a source check
   (`internal/person/application/rls_querier_test.go`) at the layer where the mistake is made.

**No index shipped, and none is warranted:** every filtered path is already index-backed, and the
residual cost is scan width and probe count (how many candidate rows a page must walk), which no
index on `person_persons` changes. The M56 stage-board `Migrated` cell stays `➖`.

Honest residuals, recorded rather than smoothed over:
- The scoped unfiltered baseline at mid reach went **19–28 ms → 41–47 ms**. The pre-M56 query was
  cheaper because it was *wrong*: it never touched `person_persons`, so it returned soft-deleted
  person ids that the hydration step then silently dropped, yielding a short page with a
  `nextPageToken`. The join buys that fix and the whole facet vocabulary.
- `unitId` at mid reach (**134 ms**) is the one case above the 100 ms per-page target set for this
  ticket. It is a 0.66%-selective filter over 10⁶ rows; closing it means a second dispatched query
  shape, which is deliberately not attempted on one data point.

### M56 ticket 3 — top-level list endpoints (2026-07-29)

`listMemberships` / `listOrders` / `listDocuments` and their facet filters. Same harness, extended
again: `scripts/seed-scale -enrich` now also gives memberships a status mix and an `effective_from`
spread, and **creates the order and document worlds, which did not exist at all** — 200 004 orders
(10% draft, so `issued_on` has a real NULL population) with one item each, and 600 364 documents
(25% with no expiry). Without them every order/document predicate would have been 0%-selective and
the measurement meaningless — the finding ticket 2 recorded, applied before rather than after.

Measured through the real Go services (`TestTicket3Scale*`,
`internal/membership/facet_scale_integration_test.go`), first page of 50:

| Filter | admin | leaf (reach 1) | mid (658) | root (100k) |
|---|---|---|---|---|
| memberships, unfiltered | 2.1 ms | 6.1 ms | 32.9 ms | 196.8 ms |
| memberships, `status=ended` | 7.6 ms | 3.2 ms | 15.1 ms | 179.7 ms |
| memberships, `unitId` | 0.3 ms | 2.8 ms | 8.3 ms | 179.2 ms |
| memberships, `effectiveFrom` range | 0.4 ms | 3.3 ms | 16.9 ms | 181.7 ms |
| orders, unfiltered | 1.6 ms | — | 78.8 ms | 185.0 ms |
| orders, `status=draft` | 0.4 ms | — | 12.6 ms | 181.5 ms |
| orders, `orderTypeId` | 78.1 ms | — | 49.9 ms | 210.0 ms |
| documents, unfiltered | 1.5 ms | — | 59.4 ms | 179.0 ms |
| documents, `typeId` | 0.7 ms | — | 48.0 ms | 178.8 ms |
| documents, `expiresOn` range | 0.6 ms | — | 31.4 ms | 184.6 ms |

**No path sequential-scans its driving table** — asserted, not observed, by
`TestTicket3ScaleNoSeqScan`, which EXPLAINs each shape (both plan shapes for every scoped path) and
fails on a `Seq Scan`. It runs the **shipping SQL**, extracted from the generated `*.sql.go` at test
time rather than pasted, so it cannot quietly stop guarding the query it names.

**One finding changed the design, and it is the whole reason this ticket ships two plan shapes per
scoped endpoint.** The first implementation folded reach as a materialized set
(`unit_id IN (SELECT authz_readable_units(subject))`). It is excellent at small reach and collapses
at root:

| | set form | point probe |
|---|---|---|
| leaf reach (1) | 1.3 ms | 2 500 – 13 100 ms |
| mid reach (658) | 27 – 122 ms | 128 – 162 ms |
| root reach (100 000) | 640 – 6 400 ms | 3.6 – 6.3 ms |

At root the set form makes the planner drive from the *reach* side: it materializes 919 286 readable
persons, joins to 551 915 documents and top-N sorts — so the `LIMIT 51` never terminates early
(documents measured **6 419 ms**). The point probe
(`authz_unit_readable_by(unit, subject)`, migration 0017) leaves the driving table in keyset order
and asks per candidate row, which is right when nearly every row qualifies and catastrophic when
almost none do. Neither shape is safe alone, so the adapters **dispatch on reach cardinality** —
precisely the sparse/dense split R-02.1 already found for `VisiblePersonIDsForSubject*`, at the same
threshold. Root-reach costs after the dispatch: documents 6 419 → 179 ms, orders 1 137 → 210 ms,
memberships 1 937 → 197 ms.

**A migration shipped, unlike ticket 2.** `0017_facet_lists.sql` adds three reach functions
(`authz_readable_units` set form, `authz_unit_readable_by` point probe,
`authz_readable_unit_count` dispatch probe — one definition each, so the parity contract with the Go
PDP oracle is not copied a fourth and fifth time) plus status-agnostic keyset indexes. The indexes
are load-bearing rather than precautionary: every pre-existing `membership_memberships` index is
**partial on `status='active'`**, and the top-level list deliberately ships no implicit status
filter, so `status=ended` and the unfiltered listing matched no index at all. The M56 stage-board
`Migrated` cell moves `➖ → ✅`.

Honest residuals, recorded rather than smoothed over:
- **A ~180 ms floor at root reach on every scoped path**, visible even where the result is 7 rows.
  It is the reach-cardinality dispatch probe, and it is **not new**: the equivalent shipped query
  `CountReadableUnitsCapped`, which the person directory has paid since R-02.1, measures **248 ms**
  on the same subject — the new `authz_readable_unit_count` is slightly cheaper. The cost is the
  `UNION`'s sort of 100 000 rows before the cap can apply; `UNION ALL` would stream, at the price of
  counting duplicate grants. Left alone deliberately: fixing it means touching person's measured and
  green path, which is outside this ticket. Recorded as an open seam.
- `orders/orderTypeId` at admin (**78 ms**) is the slowest admin shape — an `EXISTS` over 200 004
  items. It is index-backed (`order_order_items_type_order_idx`) and the cost is probe count, not
  plan shape.

### M57 ticket 1 — the dashboard aggregates (2026-07-29)

`GET /person/v1/stats/persons` and `GET /tenant/v1/stats/units`: every selected facet's distribution
plus `totalCount`, in one round-trip. Measured through the real Go services
(`internal/person/stats_scale_integration_test.go`, `TestStatsScale*`) on the same M46 world —
10^6 persons / 10^5 units, `seed-scale -enrich`.

| Request | admin | leaf (reach 13 persons) | mid (6 055) | root (919 286) |
|---|---|---|---|---|
| all 7 facets, unfiltered | 6 019 ms | 245 ms | 873 ms | 14 515 ms |
| all 7 facets, `sex=female` | 3 610 ms | 61 ms | 275 ms | 11 231 ms |
| all 7 facets, `unitId` subtree | 1 620 ms | — | — | — |
| `facets=` (totalCount alone) | 339 ms | 5.9 ms | 64 ms | 6 576 ms |
| one enum facet (`sex`) | 514 ms | — | — | — |
| one ref facet (`unitId`, top-N over 10^5 units) | 2 075 ms | — | — | — |

**The measurement settled a design question, which is why it was taken first.** A LIST needs two
reach plan shapes because a `LIMIT` cannot terminate early once the planner drives from the reach
side (ticket 3, above). An AGGREGATE has no `LIMIT` to spoil, so the prediction was that the set form
wins everywhere and the dashboard needs **one** scoped query rather than a dispatched pair. Measured
directly, counting the visible directory three ways:

| | set form (`unit_id IN (SELECT authz_readable_units(s))`) | point probe (`authz_unit_readable_by(unit, s)`) |
|---|---|---|
| leaf reach | **8.3 ms** | 12 926 ms |
| mid reach | **79.8 ms** | 17 066 ms |
| root reach | **7 144 ms** | 24 869 ms |

The set form wins at every reach — by 3.5× at root and by three orders of magnitude at leaf — so the
sparse/dense dispatch the lists carry is deliberately **absent** here, and its ~180 ms cardinality-probe
tax with it. This is the inverse of the ticket-3 table on purpose: same two shapes, opposite verdict,
because the question changed from "fill a page" to "count everything".

**Honest residual: a root-reach dashboard is not interactive.** 14.5 s for all seven facets over
919 286 visible persons is far past any UI budget, and `totalCount` alone still costs 6.6 s — the
reach semi-join over 10^6 persons, not the aggregation. Three things are true about it:

- it is **exact**, and deliberately so: D-ObjectFacets promises `totalCount` equals what exhaustively
  paging the same list returns, which is the property the differential test asserts. An estimate
  would break that promise silently;
- the `facets` CSV is a real lever the console has (`facets=` → count only, one facet → a fraction of
  the cost), and M57 ticket 3's dashboards request what they draw rather than everything;
- the estimation fallback ([facets.md](facets.md) § Open seams) is now **measured rather than
  hypothetical**: it is worth doing for the unfiltered, whole-world case only, where `pg_class.reltuples`
  is meaningful — and it must not be applied to a filtered or scoped count, where it would be wrong
  rather than approximate. Not attempted in this ticket.

Ordinary deployments are nowhere near this: the mid-reach subject (6 055 visible persons) draws a
full seven-facet dashboard in **873 ms**, and a leaf subject in **245 ms**.

### M57 ticket 2 — the membership / order / document dashboards (2026-07-30)

`GET /membership/v1/stats/memberships`, `/order/v1/stats/orders`, `/document/v1/stats/documents`,
completing the five operational-core types. Same harness (`TestTicket2Stats*`,
`internal/membership/stats_scale_integration_test.go`) on the same M46 world — 10^6 memberships,
200 004 orders, 600 364 documents.

| Dashboard | admin | leaf reach | mid reach | root reach |
|---|---|---|---|---|
| memberships, all 5 facets | 9 582 ms | 7.4 ms | 74.8 ms | 11 075 ms |
| memberships, `totalCount` alone | 316 ms | 1.9 ms | 23.5 ms | 2 343 ms |
| orders, all 4 facets | 1 121 ms | 12.5 ms | 125 ms | 2 079 ms |
| orders, `totalCount` alone | 57.7 ms | 2.1 ms | 8.1 ms | 749 ms |
| documents, all 5 facets | 1 243 ms | 17.1 ms | 201 ms | 6 145 ms |
| documents, `totalCount` alone | 209 ms | 3.7 ms | 55.4 ms | 5 238 ms |

Filtered shapes — what an operator clicks into from a chart segment: `memberships status=ended` (admin,
80 714 rows) **820 ms**; `memberships unitId` (mid reach) **9.7 ms**; `orders status=draft` (admin,
20 072 rows) **199 ms**; `documents status=active` (mid reach) **76 ms**.

**The set-vs-probe verdict was re-measured per table, not inherited.** Ticket 1 proved the materialized
reach set beats the point probe for aggregates on *person*, whose reach lands on the membership row.
`document` is the case that broke the set form for LISTS (6 419 ms at root reach, because reach runs
through the holder), so assuming the result would have been exactly the wrong move:

| count of visible rows | set form | point probe |
|---|---|---|
| documents, leaf reach | **25.7 ms** | 12 447 ms |
| documents, mid reach | **218 ms** | 15 771 ms |
| documents, root reach | **4 322 ms** | 23 651 ms |
| memberships, leaf / root | **13.2 / 2 201 ms** | 12 827 / 24 513 ms |
| orders, leaf / root | **16.0 / 801 ms** | 2 594 / 4 092 ms |

The set form wins everywhere, including the holder semi-join. So all three ship ONE scoped query, and
none pays the ~180 ms reach-cardinality dispatch tax the lists pay.

**The one number worth arguing with: `link__member_of` all-facets at 9.6 s admin / 11.1 s root.** It is
almost entirely ONE facet — `personId`'s top-N costs **8.6 s on its own**, because the window function
must rank 10^6 distinct persons to keep fifteen. Two things follow, and the second is the point:

- the same dashboard **as facets.md actually draws it** (`status`, `effectiveFrom`, `positionId` — status
  tiles, the intake curve, the billet-fill split; there is no "top persons" chart) costs
  **1 342 ms admin / 3 229 ms root / 29.7 ms mid**, measured as `TestTicket2StatsDashboardAsDrawn`. The
  `facets` CSV is not a nicety; it is the difference between 11 s and 3 s;
- the fix, if a high-cardinality ref chart is ever wanted, is a **bounded** top-N (`ORDER BY n DESC
  LIMIT k` is a heapsort, O(N log k)) instead of a full `row_number()` sort — at the price of a second
  row per facet carrying the group total, since `(other)` cannot otherwise be derived for a multiplying
  facet. Costed and left undone deliberately: it changes the ref-branch contract for all five types, and
  no shipped chart needs it.

`MATERIALIZED` on the candidate CTE was checked rather than assumed: inlining it measured 9 562 ms
against 9 374 ms, i.e. no difference — the cost is the aggregation over 10^6 rows, not the spool. It
stays, because it makes "one scan" explicit.

**Two bugs found by the live run, both invisible to the integration suite** (which connects as a
superuser and so bypasses RLS entirely):

- **the label resolvers read the bare pool**, so any row-secured label table silently resolved NOTHING.
  It surfaced the moment a labeler was pointed at `membership_positions` (RLS-protected): every
  `positionId` bucket came back unlabelled while `unit`, `person`, `country` and the type catalogs
  resolved fine. `tenant_units` was affected less visibly — its policy has a public-read arm, so PUBLIC
  units resolved and in-reach SHADOW ones did not, which means the pre-existing D-LinkTraversal labels
  had the same hole. `overlayLabeler`/`personLabeler` now read through `db.RequestQuerier`.
- **the arm convention was one edit away from a leak.** An empty subject means "no visibility predicate"
  in every stats query, and `pep.SubjectAuthority` returns `("", false)` for a machine subject (M51: a
  principal has no person identity and no reach), so a non-admin with no subject would have been handed
  whole-instance counts. Ticket 1's tenant transport already encoded that collapse itself. The rule now
  lives once in `stats.Compute`, which all five types route through: a non-admin with no subject reads
  nothing. Asserted per module.

## Re-verification note

Anchors reference symbols, not line numbers, wherever possible. Before executing any phase:
re-run the evidence greps quoted in each finding; if a finding's evidence no longer reproduces,
mark it resolved here rather than re-fixing. When a fix amends a binding decision, edit
[decisions.md](decisions.md) in the same PR — that file remains the source of truth, this one is
a snapshot review.
