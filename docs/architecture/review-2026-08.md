# Architecture review — August 2026

**Audience: Claude Code.** Written to be executed against, not just read: every finding carries
the evidence (file + symbol, so it survives line drift), why it fails at the target scale, a fix
sketch, and acceptance criteria. Re-verify a finding's evidence before working on it — the anchors
were correct as of **2026-07-09** (uncommitted work through M50 + D-EventOutbox on `main`).

> **Status — CLOSED (2026-07-12).** All findings resolved: R‑20/R‑25 (Phase 9), R‑21 (Phase 10),
> R‑13/R‑23 (Phase 8), R‑14/R‑18/R‑19 (Phase 7), R‑22 (Phase 11), R‑24 (Phase 12). Each carries a
> **Resolved** block below with its evidence. Work is uncommitted on `main`.

**Relation to [review-2026-07.md](review-2026-07.md).** That review is **closed**: its 15 solved
findings (R‑01…R‑12, R‑15…R‑17) were each re-verified on 2026-07-09 (evidence greps re-run + the
full `go test -tags integration ./...` suite: **55 packages ok, zero failures**) and stay there as
the historical record with the measurements. The four items still open — R‑13 (remainder), R‑14,
R‑18, R‑19 — are carried **here with their original IDs**; new findings from this cycle's fresh
pass continue the sequence at **R‑20**. Roadmap phase numbering also continues (July ended at
Phase 7; this review runs Phases 7–11).

**Scope reviewed:** the oikumenea Go backend (`internal/`, `cmd/oikumenea`), the DB schema +
Atlas migrations (`migrations/`), and the hermenea companion (`internal/hermenea`,
`cmd/hermenea`, `migrations/hermenea/`). The web console and client SDKs remain out of scope.
This cycle deliberately re-reviewed the July fixes **as new code** (grant cache, live-reach RLS,
incremental closure, chunked import, outbox) and the modules July did not examine deeply
(order, document, membership, localization, rank, geo, language, the verticals, pinax).

**Scale target judged against:** unchanged — multi-org national scale: ~1M+ persons in the shared
directory, unit graphs of 10⁴–10⁵ units per large org, heavy PDP query load, reference datasets
in the 10⁵–10⁷ row range.

**Stance on binding decisions:** `decisions.md` was treated as challengeable. Findings that
contradict a recorded decision say so explicitly and name the decision; each such fix must land
as an amendment in [decisions.md](decisions.md) in the same pass.

**Overall verdict.** The July program worked: the authorization wall is gone (2.65 s → ~0 ms
guarded request, 7.4 MB → 40 B GUC), closure edits are incremental, imports are chunked and
resumable, and the structure debt items all landed — and the fixes themselves held up under
re-review (the grant cache, closure algebra, outbox, and chunked-import code are disciplined,
well-commented, and differential-tested). What remains is a different class of problem: the
system is now **fast but blind** (no application metrics at all — R‑20), the July search fix was
applied to persons only while five other typeahead surfaces kept the un-indexed pattern (R‑21),
and the crypto envelope everything sensitive rides on has a **local-dev-only key backend with no
rotation path** (R‑22) — which sharpens, rather than replaces, the carried R‑14 scope decision.
Nothing here is on the July criticals' scale; this is the residue plus what the fixes exposed.

---

## Severity index

| ID | Severity | Area | One-line |
|----|----------|------|----------|
| [R‑20](#r-20) | medium | observability | Zero application metrics: cache hit rates, outbox depth/dead-letters, import health are all invisible in production |
| [R‑22](#r-22) | medium | security/operations | Envelope encryption has only the `local-dev` static-KEK backend; no KMS provider, no rewrap/rotation path despite `key_ref` plumbing |
| [R‑21](#r-21) | medium | scalability | The R‑06 search fix wasn't generalized: five other surfaces still run unanchored `ILIKE '%q%'` scans (language, geo, education ×2, company); geo also paginates with OFFSET |
| [R‑13](#r-13) | medium | scalability | (carried remainder) Multi-replica `notify`/outbox posture unverified; no two-replica compose proof; no `overview.md` write-up |
| [R‑14](#r-14) | medium | product/compliance | (carried, decision-level) PAN/IBAN storage (PCI-DSS scope), inferred political leaning, aggregation surface — needs D-DataScope |
| [R‑24](#r-24) | low | architecture | Person purge/merge atomic fan-out has grown to ~12 modules / 18 subscriptions with no width measurement or budget |
| [R‑25](#r-25) | low | structure | hermenea has no boot-time schema-version gate (oikumenea's R‑15 fix has no sibling) |
| [R‑23](#r-23) | low | operations | Prod and dev compose files share the volume name `oik-pgdata` — the password-collision footgun already bitten in M16 is still armed |
| [R‑18](#r-18) | low | docs | (carried) CLAUDE.md drift: "implemented through M15" / "Eleven modules" vs M50 / ~26 `internal/` dirs; decision files approaching context-hostile sizes |
| [R‑19](#r-19) | low | architecture (challenge) | (carried, decision-level) D-i18n "all locales in every response" has no cap as the locale set grows |

---

## Findings

### <a id="r-20"></a>R‑20 — Zero application metrics; the system is fast but blind  **[medium]**

**Resolved (2026-07-10, Phase 9).** A thin metric surface now rides the four hot seams, read via
`metrics.FromContext(ctx)` (no constructor plumbing — the ctx fallback is the same
`DefaultMetricsRegistry` witchcraft emits from): grant cache (`authz.grantcache.hits|misses|revalidations|resets`
+ `.entries` gauge), outbox (`outbox.dispatched|retried|dead` + `outbox.pending`/`outbox.oldest_pending_age_seconds`
gauges, and a **distinct dead-letter WARN** at the `status='dead'` transition), dataimport
(`dataimport.chunk_seconds` + `dataimport.rows.merged|skipped`, tagged `object_type`, at the one central
handler call), and closure edits (`tenant.closure.edit_seconds`, tagged `op`; the slice-row histogram
was **deferred** — the repo's Extend/Shrink methods return only `error`, so latency is the proxy).
`docs/modules/platform.md` gains an **Application metrics & alarm signals** table. Tests:
`TestGrantCacheMetrics`, `TestOutboxDeadLetterEmitsCounterAndWarn`, `TestRecordBacklogReportsPendingDepth`.

**Evidence.** No file under `internal/`, `pkg/`, or `cmd/` registers or emits a single
application metric (`grep -rn "metric1\|metrics.Registry" internal/ pkg/ cmd/` → no registration
call sites; witchcraft's built-in `metric.1` request metrics are all a deployment gets).
Specifically invisible:
- `internal/authorization/application/grantcache.go` — hit/miss/revalidate/reset counts of the
  cache the entire request path now leans on (D-AuthzGrantCache). A TTL misconfiguration or an
  epoch-bump storm would look like "the DB got slow", with nothing to alarm on.
- `internal/platform/outbox/dispatcher.go` — queue depth, oldest-pending age, and **dead-letter
  count**: a `notify` event that exhausts `max_attempts` is set to `status='dead'` and then
  *nothing ever surfaces it* (no log line distinct from the retry warn, no counter, no ops query
  documented). When the first real notify producer lands (R‑13's multi-replica posture rides on
  this seam), silent dead-letters become silent data-flow loss.
- `internal/dataimport` / `internal/hermenea` — chunk apply latency, rows merged/skipped, resume
  events, connector failures. The 1M-record M49 run measured these with ad-hoc test
  instrumentation; production runs of the same pipeline report nothing.
- `internal/tenant` closure maintenance — slice sizes and edit latency (the M48 property: cost ∝
  slice, not graph — unverifiable in a live deployment).
- `audit_log` partition/retention posture — `audit.retention-months` is recorded in config
  (`internal/platform/config/config.go` `RetentionMonths`) but nothing reports "partitions older
  than the intended window exist", so the deliberate operator-act model (D-AuditRetention) has no
  reminder signal.

**Why it matters at target scale.** Every July fix converted a visible failure (slow requests)
into a fast path guarded by machinery with *new* failure modes (stale cache, stuck outbox,
stalled import, bloating audit partitions). At national scale with an operator who is not the
developer, the absence of a metrics surface means the first sign of any of these is a user-facing
incident. The review process itself demonstrated the gap: every acceptance criterion in July
needed custom test-only tracers (`db.WithQueryCounter`) because no production signal exists.

**Fix sketch.** Not a monitoring platform — one thin, deliberate surface:
1. Use the metrics registry witchcraft already ships (`witchcraft-go-server` exposes a
   `metrics.Registry` on the server context; the `metric.1` emitter is already wired) — no new
   dependency.
2. Instrument the four hot seams: grant cache (hits/misses/revalidations/resets + entry count),
   outbox (pending gauge, oldest-pending-age gauge, dispatched/retried/dead counters), dataimport
   (per-object-type chunk latency histogram + merged/skipped counters), closure edits (slice-size
   + latency histogram). Add a dead-letter WARN log with a distinct message on the `status='dead'`
   transition.
3. Document the alarm-worthy signals in `docs/modules/platform.md` (dead > 0; oldest-pending age
   > n×PollInterval; cache hit rate < ~90% steady-state; audit partitions older than
   `retention-months`).

**Acceptance.** An integration test drives each seam and asserts the counters move (the outbox
test forces a dead-letter and sees the counter + distinct log); a compose run shows the metrics in
the `metric.1` log; platform.md lists the signals and thresholds.

---

### <a id="r-21"></a>R‑21 — The R‑06 search fix wasn't generalized: five surfaces still scan  **[medium]**

**Resolved (2026-07-10, Phase 10).** D-PersonSearch is now generalized (amendment in
[decisions.md](decisions.md) → D-PersonSearch "Generalized"): migration `0037_search_generalization`
gives every surface a single GIN trigram index over a STORED `search_text` haystack — `language_languoids`
(name+code), `location_locations` (six address columns), `tenant_organizations` (code+name, shared by
institutions + companies), `education_publications` (code+title), `education_scholarships` (code+name) —
plus a GIN on the real `company_org_profiles.short_name` column. EXPLAIN on 30k synthetic + realistic
rows drove the shape: a per-column BitmapOr **and** a bare expression index both lose to a seq scan
(no `pg_stats` for an ILIKE over a bare expression → ~4% default → seq scan wins), whereas the STORED
column's real stats make the planner pick the index (verified: Bitmap Index Scan on every surface with
diverse data). Every filtered list split into an unfiltered `List<X>` + a trigram `Search<X>` query (no
`(@query='' OR …)` guard that would defeat the index), branched in each module's repository. Company's
cross-join match (org haystack + profile `short_name`) is a UNION of id-sets. Geo's three
offset-paginated queries — the last `OFFSET` in the codebase — are now keyset: `Search`/`Bbox` on `id`,
nearest-first `Near` on the `(distance, id)` pair using exact `ST_Distance` in both the order and the
cursor (the `<->` KNN operator's approximated order skipped boundary rows); distance is carried in the
opaque token. Education publications/scholarships, previously unbounded (no `LIMIT`, no page token,
ordered by `code`), gained keyset pagination (Conjure `PublicationPage`/`ScholarshipPage` with
`pageSize`/`pageToken`/`nextPageToken`). The TS SDK was regenerated (`scripts/gen-ts-client.sh`) and the
one web consumer — the `EntitySelect` publication/scholarship pickers — now requests `?pageSize=200`
(matching every sibling picker) so the new default page of 50 doesn't silently truncate the list; web
`tsc --noEmit` and the SDK drift gate are green. `grep -rn OFFSET internal/*/adapters/queries/` is now empty;
new geo integration tests page through the Near `(distance,id)` and Search `id` keyset without
duplicates; the full `go test -tags integration ./...` suite is green (56 packages). The
`person_ethnicity_types` catalog search (~634 Factbook rows, `personsensitive`) was **not** in scope —
a small reference catalog, not a person-scale table.

**Evidence.** July's R‑06 gave `person_persons` a trigram GIN + SQL-side scoped search
(D-PersonSearch). The same per-keystroke, leading-wildcard pattern survives everywhere else:
- `internal/language/adapters/queries/language.sql` — `ListLanguoids`:
  `l.name ILIKE '%' || q || '%'` over `language_languoids` — **27,177 rows seeded at first boot
  by pinax on every instance** (the one table guaranteed to be big on day one). Backs the
  `LanguagePicker` server-query typeahead.
- `internal/geo/adapters/queries/location.sql` — `SearchLocationsByText`: **six** OR-joined
  `ILIKE '%q%'` columns (locality, admin areas, street, mgrs, raw_address) over
  `location_locations`, which grows with every M32 person address and any gazetteer-fed location
  use; and both it and the spatial queries (`ListLocationsNear`, `ListLocationsInBbox`) paginate
  with `LIMIT … OFFSET` — the only OFFSET pagination left in the codebase (everything else is
  keyset), so deep pages re-scan and rows shift under concurrent inserts.
- `internal/education/adapters/queries/education.sql` — `ListInstitutions` (`code`/`name` ILIKE).
- `internal/education/adapters/queries/education_reference.sql` — `ListPublications`,
  `ListScholarships` (same pattern).
- `internal/company/adapters/queries/company.sql` — `ListCompanies`
  (`code`/`name`/`short_name` ILIKE across a join).

**Why it matters at target scale.** Identical mechanics to July's R‑06: an unanchored `ILIKE`
disables every btree index, so each surface is a sequential scan per keystroke. Language is
already at 27k rows on a fresh install; locations and companies are the tables national-scale
registries and the M30 Wikidata connector feed. The typeahead pattern multiplies the cost by
keystrokes × concurrent users.

**Fix sketch.** Generalize D-PersonSearch instead of re-deriving it per module:
1. `pg_trgm` is already enabled (migration `20260601000005_person.sql`). Per surface: a GIN
   trigram index — languoids on `name` (code lookups already hit the `code` btree via the
   anchored `code ILIKE q || '%'` branch), locations on a generated concatenated `search_text`
   like person's (6 columns → one haystack), institutions/companies/publications/scholarships on
   `name` (+`short_name` via the same generated-column trick where multi-column).
2. Replace geo's `OFFSET` with keyset on `id` (the queries already `ORDER BY id`; the cursor
   machinery exists in every other module).
3. Measure before splitting indexes per column — one generated-column index per table is the
   default; per-column only if EXPLAIN demands it.

**Acceptance.** `EXPLAIN (ANALYZE)` on seeded tables (27k languoids real; 10⁵ synthetic locations
/ companies) shows bitmap index scans for a 3-char query on every surface; no `OFFSET` remains in
`internal/*/adapters/queries/`; existing list/typeahead integration tests green.

---

### <a id="r-22"></a>R‑22 — Envelope encryption: local-dev-only key backend, no rotation path  **[medium]**

**Resolved (2026-07-11, Phase 11).** Key rotation is now an executable operation (D-CryptoProvider
amended; runbook in [upgrade-safety.md](upgrade-safety.md)). Two pieces, **no schema change**:
- **Rotation-capable provider** (`pkg/crypto/localdev.go`): a KEK set with one **active** (Wrap +
  KeyRef) + N **previous** (Unwrap-only) keys; `Unwrap` tries active-then-previous and lets AES-GCM's
  auth tag pick the key, so the interface is unchanged. `crypto.local-dev.previous-keks` wires it; a
  real KMS backend slots behind the same seam later.
- **`oikumenea rewrap` CLI** (`cmd/oikumenea/rewrap.go`, operator-host-gated): a code-defined registry
  of **all 7 envelope tables** (guarded complete by `TestRewrapTablesMatchSchema`), re-wrapping every
  DEK onto the active KEK — **payload ciphertext untouched**, batched, and resumable/idempotent (the
  `key_ref <> active` predicate makes a re-run or `kill -9` resume). `--dry-run` = per-`key_ref`
  census; `--reindex-blind-index` = the heavier blind-index rotation (decrypt + recompute, correct
  because every module seals and blind-indexes the same bytes).

**Proof.** Unit: `TestKeyRotationRewrap` (A-sealed value opens after rotation, re-wraps onto B, and a
B-only provider then opens the re-wrapped envelope but NOT the original — the row truly moved off A) +
`TestBlindIndexReindexAfterKeyChange` + `TestRewrapTablesMatchSchema`. **e2e (real Postgres,
`TestRewrapCLI_Integration`):** seal 300 rows under A → rotate → `rewrap` in batches → 0 rows left on
A, all decrypt under a B-only provider; re-run = no-op; 100 fresh A-rows resume; `--reindex-blind-index`
recomputes all 400 blind indexes under a new key. This closes the PCI Req 3.6 gap that D-DataScope
flagged as the retained-PAN must-fix. **Gotcha:** the 7 tables use two column-naming styles
(`value_*`/`wrapped_dek` vs `iban_`/`pan_`/`party_`/`leaning_` prefixes) — the registry spells each out.

**Evidence.**
- `pkg/crypto/` contains exactly `crypto.go` + `localdev.go`: the `KeyProvider` seam
  (D-CryptoProvider) has **one** implementation — a static KEK decoded from base64 install
  config (`crypto.go` ~line 563: provider selection `case "local-dev":` is the only arm, and the
  default). Every production deployment holding IBANs, PANs, ethnicity, political leaning,
  personality profiles runs on it.
- `crypto.go` ~line 40: `KeyRef` is persisted with every ciphertext "so a later rotation/rewrap
  knows which key wrapped it" — but `grep -rn "rewrap\|Rewrap" pkg/ internal/ cmd/` finds **no
  rewrap code path, CLI, or plan**. The plumbing anticipates rotation; nothing can execute one.
- The blind-index HMAC key (`NewCipher`) has the same posture: static, from install config, no
  re-index path.

**Why it matters.** This is the operational half of the carried R‑14: whatever D-DataScope
decides, the data that stays encrypted is only as protected as a KEK that (a) sits base64-encoded
in install config on every host, and (b) **cannot be rotated** — a suspected KEK leak today has
no remediation short of hand-written SQL + custom code against live tables. Envelope encryption
was chosen precisely so rotation would be cheap (rewrap the wrapped DEKs; never touch payload
ciphertext) — that dividend is unclaimed. For PANs specifically, PCI-DSS Req 3.6 mandates
documented key-management including rotation; the current answer is "impossible", which forces
R‑14's PAN decision toward truncation regardless of product preference.

**Fix sketch.**
1. `oikumenea rewrap` CLI (sibling of the existing `seed` / `recover-admin` commands): walk every
   envelope-encrypted table by `key_ref`, unwrap each DEK with the old provider, wrap with the
   new, update `key_ref` — batched, resumable (the M49 cursor pattern), payload untouched. The
   table list is greppable (`key_ref` columns) and small (~8 tables).
2. A second `KeyProvider` for real deployments — file-based KEK with version suffix is enough as
   a first step (rotation = new key file + rewrap); a cloud-KMS provider can come later behind
   the same seam. Record the provider policy + rotation runbook as a D-CryptoProvider amendment
   in decisions.md and in `docs/architecture/upgrade-safety.md`'s operator guarantees.
3. Blind-index key rotation is harder (values must be re-derived from plaintext, so it needs a
   decrypt-recompute pass) — fold its (rarer) runbook into the same CLI as an explicit flag.

**Acceptance.** e2e in dev: seal N rows under key A, run `rewrap` to key B mid-write-load, all
rows decrypt, `key_ref` counts show 0 remaining on A, kill −9 mid-run resumes; decisions.md
records the provider/rotation policy.

---

### <a id="r-13"></a>R‑13 — Multi-replica story: the `notify`/scale-out remainder  **[medium, carried]**

**Resolved (2026-07-11, Phase 8).** The multi-replica posture is now proven across real OS processes
and documented. `scripts/scale-e2e.sh` (env-gated `OIKUMENEA_SCALE_E2E=1`, isolated project
`oik-scale` + its own volume, repeatable) boots **two** oikumenea replicas against one Postgres +
one shared outbox via `docker-compose.scale.yml` (`--scale app=2`) and asserts end-to-end — all four
green in a real run: **(1)** exactly one boot-seed pass (advisory lock serialized first-admin bootstrap
+ pinax autoseed; one replica seeded, the other waited-then-skipped); **(2)** 2000 notify events
drained from the shared outbox by **both** replicas (`distinct replica = 2`) each delivered
**exactly once** (`delivered = 2000`, `pending = 0`); **(3)** `kill -9` of one replica mid-drain →
the survivor redelivered the killed replica's claimed-but-unmarked rows to exactly 2000 (at-least-once
→ idempotent exactly-once); **(4)** an instance-admin grant on replica B flipped a PDP decision on
replica A within the 2 s `grantCacheTTL` (convergence through the shared `authz_epoch`, no cache-bus).
The outbox proof rides an **env-gated self-test seam** (`cmd/oikumenea/outbox_selftest.go`: a counting
notify handler + `outbox-selftest-enqueue` subcommand, inert unless `OIKUMENEA_OUTBOX_SELFTEST=1`; no
schema migration — the app role has no DDL right, so the harness provisions the deliveries table as the
superuser) — the first exercise of the previously-empty notify seam. R-20's outbox metrics make the
assertions observable. `docs/architecture/overview.md` gains a **"Multi-replica posture (scale-out)"**
section (replica-safe / per-process / operator rules), written from what the run proved. R-23 (below)
landed in the same pass (the compose volume rename).

**What already landed (July, verified).** Advisory-locked boot seeding (`db.LockBootSeed`; one
seed pass proven under `--scale app=2`), hermenea `worker.concurrency` (4-way SKIP LOCKED
parallelism proven), idempotent-per-window scheduler enqueue
(`cron:<source>:<window>` + `ON CONFLICT DO NOTHING`), and the outbox dispatcher claiming
`FOR UPDATE SKIP LOCKED` (replica-safe by construction, like the hermenea worker).

**What remains open.**
1. **The multi-replica `notify` posture is designed but unproven**: no compose run has ever
   executed two oikumenea replicas *dispatching from the same outbox* (the dispatcher's
   SKIP LOCKED claim is replica-safe on paper; the at-least-once/no-starvation behavior under two
   pollers has no test). The grant cache's cross-replica convergence is integration-tested at the
   epoch level but not in a real two-process run.
2. **No two-replica compose verification** beyond the M49 fresh-boot seed race
   (`docker-compose.scale.yml` un-publishes host ports for `--scale app=2` boot; nothing exercises
   steady-state traffic, cache convergence, or outbox sharing across replicas).
3. **`overview.md` has no multi-replica posture section**: what is replica-safe (stateless
   handlers, SKIP LOCKED consumers, advisory-locked boot), what is per-process (grant cache with
   ≤2 s convergence; outbox poll timing), and what the operator must know (session-affinity not
   required; scale hermenea and oikumenea independently).

**Fix sketch.** One compose scenario (`--scale app=2` + traffic): assert single seed pass
(already proven), grant revoke on replica A enforced on replica B within TTL, outbox events
enqueued via both replicas each dispatched exactly-once-observably (idempotent handler counts),
kill −9 of one replica mid-dispatch redelivers on the survivor. Then write the `overview.md`
section from what the run proved. (R‑20's outbox metrics make the assertions observable — do
R‑20 first or together.)

**Acceptance.** The compose scenario passes and is repeatable (env-gated like `OIKUMENEA_LANG_E2E`);
`overview.md` documents the posture; this finding closes.

---

### <a id="r-14"></a>R‑14 — Product-scope creep with concrete compliance liabilities  **[medium, decision-level, carried]**

**Resolved (2026-07-11, Phase 7).** Both halves landed.

*Decision half.* The user made the two product calls: **keep** the recoverable full PAN (not
truncate) and **re-frame** the product as a registry platform. Both are recorded in the new
**[D-DataScope](../architecture/decisions.md)** block — it enumerates the permitted data classes as
*the* boundary, **owns** the PCI-DSS CDE scope as an operator responsibility, states the
per-`pii:special` permission-code aggregation rule, and records **key rotation (PCI Req 3.6) as a
named must-fix = R‑22 (Phase 11)** — so keeping the full PAN *sharpens* R‑22 rather than closing it.
The identity re-frame propagated to CLAUDE.md + README (R‑18).

*Audit + fix half.* The permission-code audit found the person module was the one violator: a single
`person.read` (held by the base `unit-reader`) gated **every** person `pii:special` read
(ethnicity, inferred political leaning, party membership) — the exact aggregation the rule forbids —
while religion (`affiliation.manage`) and finance (`finance.read`) were already isolated. Fixed by
giving the three Art.9 person reads their **own** codes (`person.ethnicity.read`,
`person.political_leaning.read`, `person.party_membership.read`), gating the transport endpoints on
them, and adding a standalone additive **`sensitive-reader`** base role that bundles exactly those
three — **not** folded into `unit-reader`/`-manager`/`-admin`, so no graduated role unlocks the
aggregation; reading Art.9 person data is now always an explicit grant. No migration (code-defined
permissions + idempotent base-role reseed). Proven by `TestSensitivePersonReadsGatedByOwnCodes`
(`internal/authorization/domain/datascope_gating_test.go`). Residual, recorded in D-DataScope:
`person_persons.attributes` is `pii:special` and rides core `person.read` — bounded by **policy**
(Art.9 content belongs in the typed special tables, not the free-form bag), not a further split.

Carried from [review-2026-07](review-2026-07.md#r-14); reproduced as of 2026-07-09, now closed.

**Evidence.** The stated identity (CLAUDE.md, `overview.md`) is "a generic personnel &
authorization service (Keycloak-like)". The schema also holds: envelope-encrypted **payment card
PANs** (`migrations/20260601000034_finance.sql`, D-Finance), IBANs, crypto wallets, **inferred**
political leaning (M35, `pii:special`), self-declared ethnicity, religious affiliation,
watchlist/sanctions matches. That is an intelligence/registry platform's data surface.

**Why it matters architecturally (not a moral judgment — the user owns product scope).**
1. **PAN storage puts every deployment in PCI-DSS CDE scope.** Envelope encryption does not
   de-scope: the whole Postgres, the oikumenea binary, its host, and arguably the operator's
   network segment become auditable CDE. If there is no real "recover the full PAN" requirement,
   store issuer BIN + last4 + a blind index — D-Finance already built the blind-index machinery,
   so this is a column diet, not a redesign. (New this cycle: R‑22 shows key rotation is currently
   impossible, which PCI Req 3.6 requires — strengthening the truncation recommendation.)
2. **Inferred special-category data** (political leaning) has a materially different GDPR posture
   than declared data; the code isolates it well (encrypted, one-active, crypto-erased, never
   merged with declared party). The residual risk is *aggregation*: one `person` read surface can
   join ethnicity + religion + politics + finances + location. An access-tier gap here is a
   catastrophic breach — which is why the R‑09 `personsensitive` split and per-module permission
   codes matter beyond hygiene.
3. The identity drift belongs in the docs: either re-state the product ("personnel directory +
   registry platform") or record a boundary decision for what data classes are in/out.

**Fix sketch.** A `D-DataScope` decision block: what the instance is allowed to hold, PAN policy
(truncation+blind-index recommended unless a requirement is named), and a check that every
`pii:special` surface sits behind its own permission code (verify in the person-family transport:
if a single read permission unlocks the whole aggregation surface, split the codes).

**Acceptance.** Decision recorded; PAN either justified in the decision or migrated to
truncated+blind-index form (expand/contract); overlay/sensitive reads verified to sit behind
distinct permission codes.

---

### <a id="r-24"></a>R‑24 — Person purge/merge atomic fan-out: 12 modules, no width budget  **[low]**

**Resolved (2026-07-12, Phase 12).** The fan-out width now has a recorded baseline + two tripwires, so
it can't grow invisibly. (1) **`TestPurgeWidthBudget`** (`internal/person`, integration) seeds a
representative person across both erase paths (personprofile hard-delete + personsensitive crypto-erase:
citizenship, email, ethnicity, crypto wallet, personality, political leaning), wraps **only** the purge
in `db.WithQueryCounter`, logs statements + wall, and asserts a **generous budget** — measured baseline
**35 statements** (~13 ms), budget **80** (~2×), a step-change tripwire not a tight lock. (2)
**`TestPersonFanoutWidthGuard`** (`cmd/oikumenea`, no DB) pins the wired subscription count at **18**
(8 `SubscribePersonPurge` + 10 `SubscribePersonEvents`) by reading `main.go` — a module joining the
fan-out fails it, directly fixing the "M31–M45 added subscribers with no number moving" complaint.
[patterns.md](patterns.md) records the baseline + what is auto-guarded (person-family SQL width + the
subscription count) vs decision-gated (the full cross-module SQL width, which would need most of the
composition root — left to the D-EventOutbox rule). No production code changed.

**Evidence.** `cmd/oikumenea/main.go` wires **18** `SubscribePersonEvents`/`SubscribePersonPurge`
calls across ~12 modules (membership, order, authorization, document, education, company, vehicle,
finance, religion, identityfederation, personprofile, personsensitive) — all `atomic`-class, i.e.
inside the one purge/merge transaction. D-EventOutbox correctly made "adding an `atomic`
subscriber" a decision-level change, and the semantics are right (PII erasure must be atomic with
the purge). But nothing *measures* the width: no test or bench records purge latency / lock count
as modules accrue, so the documented decision gate has no signal behind it — M31–M45 each added
subscribers without any number moving.

**Why it matters.** A person purge at national scale locks rows across ~30+ tables in one
transaction. The failure mode is not correctness (it's tested) but tail latency and deadlock
surface growing invisibly, one milestone at a time — exactly the pattern the July review existed
to catch early.

**Fix sketch.** Small: extend the existing purge integration test with the query-counter tracer
(`db.WithQueryCounter`) to record statements + wall per purge, and assert a generous budget
(fails only on step-change regressions, e.g. >2× the recorded baseline). Note the current number
in `patterns.md` next to the atomic/notify classification so the decision gate has a baseline.

**Acceptance.** Purge test records and asserts the budget; patterns.md states the current width
and the budget rationale.

---

### <a id="r-25"></a>R‑25 — hermenea has no boot-time schema-version gate  **[low]**

**Resolved (2026-07-10, Phase 9).** hermenea now mirrors oikumenea's R‑15 gate, self-contained on the
hermenea side: `migrations/hermenea/embed.go` (`//go:embed *.sql`), a new marker migration
`0006_schema_version.sql` (`hermenea.schema_version`, bumped by future migrations), and
`internal/hermenea/db/schemaversion.go` (`ExpectedSchemaRevision` derived from the embedded bodies +
`ReadSchemaRevision`). `cmd/hermenea/main.go`'s InitFunc fails closed on mismatch/absence, naming
expected vs found + the `atlas migrate apply --env hermenea` remedy. e2e-proven: a DB migrated only
through 0005 aborts boot; a fully-migrated DB and a remediated DB boot OK. Adding a hermenea migration
needs no Go change (`TestExpectedSchemaRevisionDerivedFromEmbed`).

**Evidence.** oikumenea derives `ExpectedSchemaRevision` from its embedded `migrations/atlas.sum`
and refuses to serve against a stale schema (`internal/platform/db/schemaversion.go`, July R‑15).
hermenea has no equivalent: `internal/hermenea/db/` contains only `pool.go` — no schema-version
check of any kind against `migrations/hermenea/` (July's R‑15 fix explicitly said "the hermenea
binary should do the same if it has an equivalent gate"; it has none at all).

**Why it matters.** A hermenea binary deployed against an un-migrated hermenea DB (e.g. the M49
`0005_resume_cursor` columns missing) fails at first job claim with a raw SQL error mid-run,
not at boot with a clear message — the exact footgun class R‑15 eliminated on the oikumenea side,
on the binary whose job is long unattended runs.

**Fix sketch.** Mirror the oikumenea mechanism: embed `migrations/hermenea/`, derive the expected
revision from the last entry, check a `hermenea.schema_version` row (add the tiny table in the
next hermenea migration; write it from the migrate step like oikumenea's) at pool init,
fail-closed with the same style of error.

**Acceptance.** hermenea refuses to boot against a DB missing the latest hermenea migration,
naming expected vs found; adding a hermenea migration requires no Go change.

---

### <a id="r-23"></a>R‑23 — Prod and dev compose share the `oik-pgdata` volume name  **[low]**

**Resolved (2026-07-11, Phase 8).** `docker-compose.yml` now names its data volume **`oik-pgdata-prod`**
(the mount + the `volumes:` declaration), distinct from the dev stack's `oik-pgdata`
(`docker-compose.dev.yml`, unchanged), with a comment at the `volumes:` block explaining why. A prod
`up` after a dev `up` in the same directory now initializes its own cluster instead of silently
inheriting the dev data dir + password. `docker compose config` parses for both the prod and the
prod+scale (`docker-compose.scale.yml`) file sets.

**Evidence.** `docker-compose.yml` (~line 31) and `docker-compose.dev.yml` (~line 42) both mount
`oik-pgdata:/var/lib/postgresql/data` and declare the volume under the same name. Run from the
same directory without `-p`, both compose files resolve to the **same** named volume — so a prod
`up` after a dev `up` inherits dev's initialized cluster and its password, and Postgres skips
init. This exact collision was hit during M16 verification (noted in the compose gotchas) and the
files still carry it.

**Why it matters.** It's a data-integrity footgun on the operator's first real deployment from a
dev machine — worst case, a "prod" stack silently running on a dev data directory.

**Fix sketch.** Rename the prod volume (`oik-pgdata-prod`) — or give the files distinct default
project names via `name:` — and add one line to the deploy docs. Nothing else changes.

**Acceptance.** Fresh `docker compose -f docker-compose.yml up` after a prior dev `up` in the
same directory initializes its own cluster; both stacks list distinct volumes in `docker volume ls`.

---

### <a id="r-18"></a>R‑18 — CLAUDE.md / docs drift  **[low, carried — evidence updated]**

**Resolved (2026-07-11, Phase 7).** CLAUDE.md's status header now reads "verified through M50;
review-hardening in progress" (was "implemented through M15, M16 in implementation"), points at the
stage board as authoritative over the header, and names the review-2026-08 cycle as the current
work; the identity line is re-framed to a "personnel directory + multi-domain registry / intelligence
platform" (the R‑14.3 decision — [D-DataScope](../architecture/decisions.md)) with the same re-frame
in [`docs/README.md`](../README.md); the "Eleven modules" map now labels the eleven **core** modules
and adds the M18–M50 vertical/enrichment/reference set (geo, language, education, company, religion,
vehicle, externalorg, finance, personprofile, personsensitive, pinax + the dataimport/watchlistclient
seams), pointing at the stage board + per-module docs as the census. Both `decisions.md` and
`roadmap-decisions.md` now open with a compact **decision index table** (ID → one-liner → anchor) so
an agent loads the index and fetches only the blocks it needs. Docs link check green.

**Evidence (re-verified 2026-07-09).** CLAUDE.md still opens with "implemented through M15, M16 in
implementation" and "Eleven modules"; reality is M50 verified (stage board current through M50 —
`docs/milestones.md` is **not** drifted; the drift is CLAUDE.md specifically) and **26**
directories under `internal/` (~20 real modules beyond the listed eleven: company, education,
externalorg, finance, geo, language, pinax, religion, vehicle, dataimport, watchlistclient,
personprofile, personsensitive, …). `decisions.md` and `roadmap-decisions.md` have kept growing
past July's measured 1,684/1,732 lines and remain context-window-hostile for their stated
audience (Claude Code) without an index.

**Fix.** Refresh CLAUDE.md's status header and module map (mechanical); add a compact decision
**index table** (ID → one-liner → anchor) at the top of both decision files so an agent can load
the index and fetch only the relevant blocks.

**Acceptance.** CLAUDE.md names the real module set and current milestone position; both decision
files open with an index table; the docs link check stays green.

---

### <a id="r-19"></a>R‑19 — D-i18n "all locales in every response" is uncapped  **[low, decision challenge, carried]**

**Resolved (2026-07-11, Phase 7).** The user chose the **`locales=` projection param** over a hard
locale-count cap. D-i18n is amended in [decisions.md](decisions.md): all-locales stays the default,
and a client MAY pass a repeated `locales=ukr&locales=eng` query param to trim every translatable
label map to that subset (intersection with what is stored) — a payload-size projection, explicitly
**not** Accept-Language negotiation (absent/empty ⇒ all locales). Demonstrated end-to-end on `GET
/platform/v1/colors` (`listColors`): `locales` added to `api/platform.conjure.yml`, Conjure + Go/TS
clients + OpenAPI regenerated, projection applied in `internal/platform/transport/catalog.go`
(`localeProjection`/`projectLocales`, unit-tested by `TestLocaleProjection`). Same pass closed the
noted perf nit: `DefaultLocale` (consulted per translatable field on every response) is now cached
with a 30 s TTL + write-invalidation instead of a per-invocation `ListLocales` round-trip. Gotcha for
future list query params: the conjure plugin mis-generates `optional<list<string>>` query args
(`[]string != ""`) — use a plain `list<string>` (absent ⇒ empty slice).

Carried from [review-2026-07](review-2026-07.md#r-19); now closed.

**Evidence.** D-i18n (decisions.md) + `LabelsByID` (`internal/localization/application/service.go`)
return a `locale → text` map for every translatable label in every response; supported locales are
admin-extensible (4 seeded since M45: `ukr`/`eng`/`spa`/`por`).

**Why it eventually matters.** Response size for list endpoints scales ×|locales| on every
translatable field. At 4 locales it's noise; an admin adding 20 locales multiplies every catalog
response ~5× with no cap. Record the boundary now: either a documented soft cap on supported
locales, or an optional `locales=` projection query param (explicitly *not* Accept-Language
negotiation, preserving the decision's intent). Also (still true as of 2026-07-09): `LabelsByID`
calls `DefaultLocale` per invocation — worth a small cache when touching this code.

**Acceptance.** The boundary is recorded in decisions.md (cap or projection param); if the
projection param is chosen, one catalog endpoint demonstrates it end-to-end.

---

## Roadmap

Phase numbering continues from July (Phases 0–6 delivered there; Phase 7 carried). Ordered by
(risk removed ÷ effort) and dependency; size each as a milestone in the existing pipeline.

### Phase 7 — Decisions & docs (R‑14, R‑18, R‑19) **[carried; needs the user]**
Unchanged scope from July: D-DataScope (PAN policy — truncation+blind-index now additionally
supported by R‑22's rotation gap), sensitive-read permission audit, CLAUDE.md refresh, decision
index tables, i18n locale-cap note. Cheap, but decision-level. R‑14's PAN outcome feeds Phase 11.

### Phase 8 — Scale-out completion (R‑13 remainder, R‑23)
The two-replica compose scenario (steady-state traffic + cache convergence + shared outbox +
kill −9 redelivery), the `overview.md` multi-replica posture section, and the compose volume
rename. Do together with (or right after) Phase 9 — the outbox assertions want R‑20's metrics.

### Phase 9 — Observability (R‑20, R‑25)
The four-seam metric surface + dead-letter visibility + platform.md alarm signals; the hermenea
boot-time schema gate. Self-contained, no schema change on the oikumenea side, one tiny hermenea
migration.

### Phase 10 — Search generalization (R‑21)
D-PersonSearch generalized: trigram GIN per remaining typeahead surface (language, geo,
education ×2, company), keyset replacing geo's OFFSET. Migrations + query changes, same shape as
July's Phase 4 — the recipe is proven.

### Phase 11 — Crypto operations (R‑22)
The `rewrap` CLI + a versioned production `KeyProvider` + the rotation runbook (D-CryptoProvider
amendment). Sequence after Phase 7: if D-DataScope truncates PANs, the rewrap surface shrinks
before the tool is built.

### Phase 12 — Purge-width baseline (R‑24)
One test extension + a patterns.md number. Can ride along with any other phase.

### Explicitly NOT recommended (unchanged from July)
- Replacing the custom PDP with OpenFGA/SpiceDB — evaluated and deferred 2026-07-03; the July
  fixes removed the actual bottleneck.
- Splitting the monolith into services — the seams are the thing to keep strengthening.
- Postgres sharding / read replicas — nothing in these findings is beyond one well-indexed
  primary; revisit only with measurements in hand.

## Re-verification note

Anchors reference symbols, not line numbers, wherever possible. Before executing any phase:
re-run the evidence greps quoted in each finding; if a finding's evidence no longer reproduces,
mark it resolved here rather than re-fixing. When a fix amends a binding decision, edit
[decisions.md](decisions.md) in the same PR — that file remains the source of truth, this one is
a snapshot review. July's measurement harness (`scripts/seed-scale`, `db.WithQueryCounter`,
`pg_stat_statements` — see [review-2026-07.md § Measurements](review-2026-07.md#measurements))
is the standing tool for any acceptance criterion here that needs numbers.
