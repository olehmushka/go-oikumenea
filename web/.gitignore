/node_modules
/.next
/out
/build
.env
.env.local
*.tsbuildinfo
next-env.d.ts.bak

# The API layer is the typed TS SDK oikumenea-client (../clients/typescript, a file: dep generated
# from the Conjure contract — D-ClientSDK). The Docker build uses a repo-root context so that package
# is in scope; see web/Dockerfile.
